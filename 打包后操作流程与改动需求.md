# 打包后操作流程与改动需求（kimi 梳理，2026-08-21）

> 梳理者：kimi k3（基于已通读的 perfdog 项目代码）
> 用途：作为 Qwen 3.8 MAX 优化任务输入（今晚 00:00 执行）
> 范围：GitHub 获取 → 启动 → 看板 → 应用列表 → 停止采集 → 数据地址，六个环节

---

## 核心痛点定位（两处，均已代码证实）

### 痛点 1：实时看板"左右横跳"（应用列表刷新时）
三处叠加导致：
1. **主因**：`web/index.html:169` — `loadApps()` 每次轮询先把下拉清空成"加载中…"占位符再重建；label 解析期间每 3s 一次，下拉宽度 140↔280px 振荡。
2. `web/assets/style.css:40-46` — `#tb-select` 弹性宽度（min 140 / max 280），选项从包名变"中文名 (包名)"即改宽。
3. 头部 flex `margin-left: auto`（style.css:27-38）把宽度变化传导到状态栏。

**修复方案**：
- CSS 固定宽度（布局根除）
- 前端**就地 diff 更新**：轮询不清空下拉，只更新变化的选项（无感刷新）
- 后端包名排序本身稳定，无需动

### 痛点 2：关闭实时看板 ≠ 停止采集
现状：唯一停止路径是 Ctrl+C；关闭浏览器对后台零影响；label 解析线程也无中止机制。

**修复方案（P0）**：
- 新增 `POST /api/stop` 端点 + 看板"停止采集"按钮
- **复用首次 Ctrl+C 的完整停止路径**（退采样循环 → 停 logcat → 生成 HTML 报告 → running=False），不新增第二套停止逻辑
- 给 label 解析加中止标志
- "浏览器关闭自动停止"降级 P1 可选（45s 宽限 + config 开关默认关，避免刷新页面误停）

---

## 六个环节改动清单

| # | 环节 | 现状 | 期望 | 改动点 | 优先级 |
|---|---|---|---|---|---|
| 1-1 | GitHub 获取 | 有 Release（exe/安装包）+ 源码仓库 | 像开源项目一样拿就能用 | README 获取章节完善；P1 可加 GitHub Actions 自动构建单文件 exe（仅构建期依赖，不违反运行时纯标准库） | P1 |
| 2-1 | 本地启动 | bat 里引用 Cherry 私有 uv 路径 | 无私有依赖 | `start_perfdog.bat` 去掉 `C:\Users\SparkGame\.cherrystudio` 硬编码路径（回退 PATH 查找） | P1 |
| 2-2 | 启动 | Python 端 webbrowser.open 时序需确认 | 服务就绪后开浏览器 | `main.py` webbrowser 打开时序修正（已由 Qwen 做过一轮，复核） | P1 |
| 3-1 | 看板 | 下拉每次轮询清空重建 → 宽度振荡 | 无感刷新、固定布局 | index.html `loadApps()` 就地 diff 更新，不清空；固定下拉宽度 | P0 |
| 3-2 | 看板 | `#tb-select` 弹性宽度 | 固定宽度 | style.css `#tb-select` 固定宽度 | P0 |
| 3-3 | 看板 | 头部 flex 传导宽度变化 | 布局稳定 | style.css 头部布局固定 | P0 |
| 4-1 | 应用列表 | label 后台解析顺序影响显示 | 固定排序（后端包名排序已稳定，保持） | 前端按固定 key 渲染 | P0 |
| 5-1 | 停止采集 | 只有 Ctrl+C；关浏览器不停止 | 看板可停止，联动停止采集 | 新增 `POST /api/stop` + 看板停止按钮；复用首次 Ctrl+C 停止路径 | P0 |
| 5-2 | 停止采集 | label 解析线程无中止 | 停止时中止解析 | label 解析加中止标志 | P0 |
| 5-3 | 停止采集 | 无自动停止 | （可选）关闭浏览器自动停止 | 45s 宽限 + config 开关默认关 | P1 |
| 5-4 | 停止采集 | 无彻底退出端点 | （可选）看板退出程序 | `POST /api/shutdown` 彻底退出 | P1 |
| 6-1 | 数据地址 | 启动提示块已加（Qwen 上轮） | 看板常驻显示数据目录 | 数据目录地址透出到看板 UI 常驻显示 | P0 |
| 6-2 | 历史列表 | 轮询全量重建 | 无感更新 | 历史列表 diff 更新 | P1 |

---

## 约束（Qwen 执行时必须遵守）
- 采集链路（fps/cpu/mem/net/therm 五线程、热切换、SSE）零改动
- 运行时纯标准库；GitHub Actions 仅构建期依赖
- 前端改动必须升 `?v=` 版本号（当前 v35 → v36）
- API 响应格式保持兼容（{"ok":..., "apps":[...], "resolving":bool} 等）
- 改完 py_compile 自查

## 与本任务并行（已单独预约今晚 00:00）
- 《代码评估与优化项目.md》的 P0/P1/P2 优化项（内存 PSS 核查、温度校验、/api/runs 缓存、FPS 只算新帧、前端 hasData 修复、事件标注线对齐、合并 adb 往返、断连恢复、golden test 等）
