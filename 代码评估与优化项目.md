# 代码评估与后续优化项目

> 评估日期：2026-08-21
> 评估方式：双模型交叉评审（kimi k3 + Qwen 3.8 MAX），Qwen 对 37 份真实采集数据（6391 个采样点）做了统计实证，凡结论均带实测支撑。
> 状态：评估结论已保留，作为**后续优化项目**待排期实施（未修改代码）。

---

## 一、总体结论

- **Qwen 总体评分：7.2 / 10**（架构 8.5 / 代码风格 8.5 / 健壮性 7 / 数据正确性 6 / 性能 7 / 测试 2）
- 工程底子扎实、文档意识出色；最大风险不在代码结构，而在**两个已实测存在的数据正确性缺陷**（内存 PSS/RSS 口径、温度单位），以及零单元测试。
- kimi 从指标算法与真机报告角度交叉印证，两模型在主要结论上一致。

---

## 二、数据正确性严重问题（优先处理）

### 🚨 1. 内存 PSS/RSS 倒挂（Qwen 实证，严重度 🔴 高）
- 5700 个双值样本中 **4140 个（72.6%）出现 RSS < PSS**，物理上不可能（单进程 PSS 恒 ≤ RSS）；中位数 PSS 比 RSS 大 69MB、比值最大 1.57。
- 疑因：PSS 正则（大小写敏感）对 Android 14 `dumpsys meminfo` 输出匹配位置存疑；且 **PSS 来自 dumpsys、RSS 来自 /proc/status，双源不同步**放大偏差。
- 建议：真机对照 `dumpsys meminfo <pid>` 修正正则（锚定 App Summary 段 `Total PSS:`/`Total RSS:`，大小写不敏感）；PSS/RSS 改同源解析；或换 `/proc/<pid>/smaps_rollup`（毫秒级，同源双值）。

### 🚨 2. 温度单位无校验（Qwen 实证，严重度 🟠 中）
- 实测 6 个采样点 `temp_c = 370.0`（电池 370°C 荒谬）——部分机型/状态温度单位不是假设的 0.1°C 而是 0.01°C（毫摄氏度），代码无条件 `/10`，无物理范围校验。
- 建议：换算后 >60°C 尝试按 ×0.01 重算，仍异常置 None 并记 error；`sys_unavailable` 锁定可加低频重探。

---

## 三、指标算法评审（kimi + Qwen 交叉印证）

| 指标 | 结论 | 要点 |
|---|---|---|
| **FPS** | 总体合理，⚠️ 缓冲残留污染 | span 法/哨兵过滤/静止判断/双通道状态机均正确；但 128 槽缓冲 2.13s 窗口 > 0.5s 采样，卡死帧留缓冲 30–60s 重复污染 P95/Max/Jank（实测 88 点 frame_max>500ms 且相同极值连续重复）→ 建议只对新增帧算 gaps；`STALE_NS` 为死代码；前后端 P95 定义差一位次；单列兜底 60Hz 高刷误判；sf/gfx Jank 口径不一致 |
| **CPU** | 算法正确 | 差值法+CLK_TCK 动态校准正确；⚠️ 进程% 为单核折算可 >100%（实测峰值 208.9%），需图例注明；guest 时间重复计入（Android 恒 0 实际无影响）；进程重启后首个差值跨死亡期 |
| **内存** | 🚨 见上严重问题 1 | dumpsys meminfo 0.5–1s 拖慢采样；PSS/RSS 双源不同步 |
| **网络** | 口径级认知错误 | `/proc/<pid>/net/dev` 实为**整机流量**（Android App 与 shell 共享 root netns），非进程级；免 root 无严格进程级方案，需修正注释与报告口径标注"整机（除 lo）" |
| **温度** | 合理 + 🚨 见上严重问题 2 | dumpsys battery 1°C 量化、更新慢、电池≠SoC；建议补 /sys/class/thermal/thermal_zone* |
| **main 调度** | 观察点 | fps 0.5s 采样但 1s 落盘，一半样本被丢弃（jsonl/SSE 实为 1s 粒度） |

---

## 四、性能优化清单（按收益/成本排序）

| # | 优化点 | 收益 | 成本 |
|---|---|---|---|
| 1 | `/api/runs` 每次请求全量读所有 jsonl 计行（现 37 文件 27MB，每 5s 轮询读 27MB）→ 按 (mtime,size) 缓存行数 | 🔴 高 | 低 |
| 2 | 内存采样换 `smaps_rollup`（毫秒级，PSS/RSS 同源，Android 8+，失败回退 dumpsys） | 🔴 高 | 低-中 |
| 3 | 合并 adb 往返（cpu/mem 各 2 次 shell → 1 次；net+pid 校验合并），每秒省 3–4 次往返 | 🟠 中 | 低 |
| 4 | `/api/report` 按路径+mtime 缓存解析结果 | 🟠 中 | 低 |
| 5 | PidResolver 单次 `ps -A` 替代 pidof + N 次 cat cmdline | 🟡 低-中 | 低 |
| 6 | FPS 百分位只算新帧（数据质量+计算量双赢） | 🟠 中 | 低 |
| 7 | 已做好的保持：SSE `_seq` 游标增量、前端 lttb/动画全关/拖动条 rAF 分级节流、扫描锁防惊群 | ✅ | — |

---

## 五、代码质量清单

| # | 问题 | 严重度 |
|---|---|---|
| 1 | 零单元测试（解析器高危裸奔）→ 建议解析函数纯化 + 10–20 条 golden test | 🟠 中 |
| 2 | 前端 `hasData` 真值判断（app.js:683 `r.fps && r.fps.fps`）：合法 0 值（静止画面）隐藏整张卡片 → 改 `!= null` | 🟠 中 |
| 3 | logcat 事件标注线按类目轴映射，未命中整数秒类目静默不渲染 → 映射到最近采样点下标 | 🟠 中（待验证） |
| 4 | PidResolver 进程复用不校验 comm；失败无节流（前台无游戏时 3 线程 ~3次/s 打 pidof） | 🟡 低 |
| 5 | 设备断连无恢复策略（5 线程吞异常空转，写满 error jsonl）→ 连续 N 次失败探活 adb devices 告警 | 🟡 低 |
| 6 | web.py 588 行单文件、Handler 内嵌闭包 → 建议拆 web_apps.py | 🟡 低 |
| 7 | 杂项：export_report `str.replace(".html")` 拼路径、logcat 跨年时间戳、限流字典膨胀 | 🟡 低 |

---

## 六、真机报告数据诊断（kimi，20260821_131418 / 20260821_083608）

### CPU 后期波动频繁的原因
**主因：游戏侧后期真实重负载场景事件；热降频/调度抖动是放大器；采集侧可排除。**
- 波动是方差变大而非均值漂移（进程 CPU 标准差 F1 前 13.88→后 18.56、F2 前 11.80→后 16.42）。
- 场景事件指纹：F1 729–737s 进程 CPU 158.98% + PSS 766MB（平时 537MB）+ 网络 980KB/s + Jank 0.22–0.51 连续 6s；F2 1258–1279s 网络 2442KB/s + 帧 Max 777ms + Jank 0.78–0.92 + FPS 跌至 0→28.94。
- 对照组：F1 t=80s frame_max 1988ms 但 CPU 仅 7–22%（加载等待型卡顿不耗 CPU），与后期"CPU 主动冲高型"性质不同。
- 热降频放大：F2 自 737s 温度钉死 43.0°C 长达 545 点，后期全部 Jank>0.2 与 FPS<30 均落在 43°C 段。

### 无内存泄漏为何温度升到 43°C
**前提纠正：内存泄漏与发热无因果关系（发热 ∝ 功耗，不 ∝ 内存占用）。PSS 平稳只说明内存健康，推不出"不该热"。**
1. GPU 持续满帧渲染（FPS 全程 57–59.86，WebGL 每 16.7ms 一帧，GPU 几乎无空闲）——最大热源之一，恰在 CPU 指标里不可见。置信度高。
2. CPU 持续中等负载（整机 33.5–42.5% 全程不降，8 核 10+ 分钟足够升温 13°C）。置信度高。
3. 热平衡平台：F2 737s 后钉死 43.0°C = 发热=散热稳态（或温控限功率）。置信度高。
4. 屏幕常亮 60Hz 稳定功耗。置信度中。
5. 排除充电：电压单调下降（4.37→4.30V）纯放电。置信度高。
6. 采集开销 <1–2% CPU，可忽略。
- 佐证：43°C 后 F2 出现 Jank 0.92/帧 Max 777ms——"发热→调度收紧→帧不稳"链已出现，临近 45°C 降频阈值。

---

## 七、后续优化项目清单（待排期）

**P0（数据可信度，先做）**
1. 内存 PSS 解析核查（真机对照 dumpsys meminfo 修正正则 / 换 smaps_rollup）
2. 温度物理范围校验（>60°C 按 ×0.01 重算，异常置 None）

**P1（体验/性能）**
3. /api/runs 按 (mtime,size) 缓存行数
4. FPS 百分位/Jank 只算新帧
5. 前端两处正确性（hasData 改 !=null、事件标注线对齐验证）

**P2（加固）**
6. 合并 adb 往返 / /api/report 缓存 / PidResolver ps -A 与 comm 校验
7. 设备断连恢复策略
8. 解析器 golden test（10–20 条真实输出）
9. 文档口径修正（网络=整机流量、CPU 多核折算说明、温度电池≠SoC）
