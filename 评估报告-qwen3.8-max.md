# 评估报告：代码质量 + 性能优化 + 指标算法评审（Qwen 3.8 MAX）

> 评估模型：Qwen 3.8 MAX（阿里云百炼）
> 评估日期：2026-08-21
> 评估方法：通读全部源码（collector/ 14 个 Python 文件约 2300 行 + web/ 前端约 1300 行 + sdk），并对 output/ 下 37 份真实采集数据（6391 个采样点、跨 8 月 12 日–21 日多次真机采集）做了统计实证，以下结论凡涉数据均有实测支撑。

---

## 一、代码质量

**总体印象**：单兵工具里少见的高水准——注释密度高且写"为什么"（如 fps.py 头部把荣耀机型的坑、方案取舍全记录了）、异常兜底意识强、模块边界清晰（metrics/ 各自独立、collector 与 web 解耦）。主要短板是零测试和几处隐蔽的正确性问题。

| # | 问题点 | 严重度 | 说明与建议 |
|---|---|---|---|
| 1 | **零单元测试**（全项目无任何 test 文件、无 requirements/CI） | 🟠 中 | 核心资产是几个二进制/文本解析器（fps/cpu/mem/arsc），且已出过递归爆内存这类回归。建议把各 collector 的**解析函数**（`_parse_latency`/`_parse_gfxinfo`/mem 正则/cpu stat 切分）与 adb 剥离成纯函数，配 10–20 条真实输出做 golden test。成本半天，收益是以后改算法不怕回归 |
| 2 | **前端 `hasData` 用真值判断，合法 0 值会隐藏整张卡片**：`app.js:683-688` `r.fps && r.fps.fps` | 🟠 中 | FPS/CPU 全为 0 是**合法数据**（静止画面/空载），但 0 是 falsy → 整张卡片被隐藏，用户误以为没采到。应改为 `r.fps.fps != null` 判断。实测数据有 875 个 FPS=0 点，若某次测试全程静止，FPS 卡会整个消失 |
| 3 | **logcat 事件标注线疑似对不齐**：`app.js:780` 事件按 `Math.round(ev.t_ms/100)/10` 映射到类目轴 | 🟠 中（待验证） | 采样行间隔 1s，类目值是整数秒；事件时刻如 10.4s 不在类目集合里，ECharts 类目轴的 markLine 对未命中类目通常**静默不渲染**。即大部分事件线可能根本没画出来。建议改为"映射到最近采样点下标"（用 rows 的 t_ms 二分找最近）。请在真实报告页验证一次 |
| 4 | **PidResolver 进程复用不校验身份**：`pidresolver.py:57-59` 只检查 `comm` 非空，不比对内容 | 🟡 低（概率小、后果重） | 进程被杀后 pid 被系统复用给别的进程时，会继续采错误进程的数据且无感知。建议 `comm.strip()` 与期望进程名前缀比对，不符则重新 resolve。另外 pid 为 None 时 `resolve()` 无节流，游戏不在前台期间 3 个采集器线程会以 ~3 次/秒频率打 `pidof`（实测 no_layer 窗口 67 个采样点），建议加失败缓存节流 |
| 5 | **设备断连无恢复策略** | 🟡 低 | 5 个采样线程把 adb 异常吞成 `{key}_error` 后继续空转，主循环照常写满屏 error 的 jsonl，用户只能看控制台发现。建议：连续 N 次全指标失败 → 探活 `adb devices`，打印醒目告警/置 web status；logcat.py 已有重连逻辑可参考 |
| 6 | **web.py 单文件 588 行、Handler 内嵌闭包**，`do_GET` 长 if/elif 链 | 🟡 低 | 当前规模尚可维护，但 device-apps/report/runs 三块已占一半篇幅。若继续长功能，建议拆 `web_apps.py`（应用列表）独立模块。`_list_runs` 每次请求重新 open 每个文件计行（详见性能节） |
| 7 | **main.py 信号处理里 `sys.exit(0)`**（`main.py:195-201`）；`stop` 用 dict 而非 `threading.Event` | 🟡 低 | 功能正确但非惯用法；SignalHandler 中 exit 依赖 SystemExit 冒泡到主线程，若主线程阻塞在 adb 调用会延迟退出。风格问题，可不动 |
| 8 | **main.py 主循环 1s 写盘丢弃一半 FPS 样本**（fps 线程 0.5s 采样，`latest` 只留最新） | 🟡 低 | 设计取舍（注释已说明为实时看板服务），但落盘数据里 FPS 有效采样率实为 1Hz。由于 span 法本身是 ~2s 窗口统计，信息损失有限；若要保留，把行写盘间隔降到 0.5s 或对 fps 做窗口聚合 |
| 9 | 杂项：`export_report.py:112` 用 `str.replace(".html", ...)` 拼事件文件路径（路径中若再含 ".html" 会错）；`logcat.py:166` 用 `datetime.now().year` 拼设备时间戳（跨年 12/31 采集会错一年）；`logcat.py` 的 `_last_emit` 限流字典无清理、长采集缓慢膨胀 | 🟡 低 | 都是边界小坑，顺手可修：`removesuffix`、年份取锚点年、字典定期裁剪 |

**亮点**（应保持）：采集器按指标分线程 + 各自间隔的设计正确解决了慢指标拖累快指标；热切换用"替换 collectors dict + worker 每轮重取"避免了复杂的线程重建；fps.py 的双通道状态机（`_ever_surfaceview` 记忆、gfx 连续 0 帧回退）看得出是真机反复调出来的，防御层次清晰。

---

## 二、性能与优化空间（按收益/成本排序）

实测基线：USB 真机、5 个采样线程，adb 单次 shell 往返约 20–40ms。

| # | 优化点 | 收益 | 成本 | 说明 |
|---|---|---|---|---|
| 1 | **`/api/runs` 每次请求全量读所有 jsonl 计行数**（`web.py:525` `sum(1 for _ in open(fp))`） | 🔴 高 | 低 | report.html 每 5s 轮询一次；output/ 现 37 文件 27MB，即**每 5 秒从磁盘读 27MB**，随采集积累线性恶化。历史 jsonl 采集结束后不可变 → 按 (mtime,size) 缓存行数，只重算变化的文件；或直接用 size/平均行长估算 |
| 2 | **内存采样换 `/proc/<pid>/smaps_rollup`**（Android 8+） | 🔴 高 | 低-中 | `dumpsys meminfo` 单次 0.5–1s，是 mem 线程 2s 周期的主要开销；smaps_rollup 一次 cat 毫秒级且同时给 Pss/Rss（还顺带解决下文指标节的 PSS/RSS 同源问题）。风险：部分 ROM SELinux 限制 shell 读他进程 smaps → 实现为"先试 rollup、失败回退 dumpsys"，在目标机型验证一次即可 |
| 3 | **合并 adb 往返**：cpu 每轮 2 次 shell（/proc/stat + /proc/pid/stat）、mem 2 次、net 1 次 + pidresolver 周期性 1 次 | 🟠 中 | 低 | 一条 `sh -c 'cat /proc/stat; echo; cat /proc/PID/stat'` 省一半往返；net+pid 校验也可合并。合计每秒省 3–4 次往返（约 100ms/s 的 adb 通道占用），给设备减负 |
| 4 | **`/api/report` 大文件缓存**：每次点开报告全文读 + JSON 解析，且历史文件不可变 | 🟠 中 | 低 | 按路径+mtime 缓存解析结果；1MB jsonl 反复点开从 ~50ms 解析降到 0 |
| 5 | **PidResolver 单次 `ps` 替代 pidof+N 次 cat cmdline** | 🟡 低-中 | 低 | 微信约 10 个子进程，resolve 一次最多 11 次往返；`ps -A`（toybox 均支持）一次拿全，本地过滤 |
| 6 | FPS 百分位改为**只算新帧**（见指标节第 2 条） | 🟠 中 | 低 | 数据质量+计算量双赢：gaps 从 127 个降到每样本 ~30 个新帧 |
| 7 | 已做得好的，不必动 | — | — | SSE 用 `_seq` 游标增量推送 + 0.2s 心跳（正确且省）；前端 `sampling:'lttb'`、动画全关、拖动条 rAF 分级节流（60fps UI/30fps 重绘）、并发扫描锁防惊群——这些都是对的标准库方案，保持 |

---

## 三、指标算法合理性（重点，逐项评审）

### 1. FPS（SurfaceFlinger --latency span 法）——总体合理，2 个实锤问题

**合理的部分**（均有实测支撑）：
- 取第 2 列 actualPresentTime、过滤 INT64_MAX 哨兵、`MAX_VALID_TS=1e17` 上限——正确，这是荣耀机型的实测坑；
- span 法 `(n-1)/跨度` 替代增量计数——正确选择：滚动缓冲满槽时增量法恒 0，span 法稳定。实测 6280 个 FPS 点无一 >65（60Hz 设备），分布健康；
- 静止判断"最新帧时间戳是否推进"——正确规避了帧时间戳与 uptime 基准不同的问题（注释也写了原因）；
- sf/gfx 双通道 + `_ever_surfaceview` 记忆 + gfx 连续 3 次 0 帧回退——状态机设计合理，实测 96.7% 走 sf、3.3% gfx，切换无误伤。

**问题**：
| # | 问题 | 严重度 | 建议 |
|---|---|---|---|
| 1.1 | **百分位/Jank 被缓冲残留污染**：gaps 对全部 ~127 个缓冲帧计算，一次 2–3.6s 的卡死帧会留在 128 槽缓冲里约 30–60s，期间**每个采样点的 P95/Max/Jank 都被重复污染**。实测：88 个点 frame_max>500ms，且 2088/3609ms 等相同极值在连续多个样本重复出现（p50 恒 16.71 而 max 2088 连出两次）——典型的残留特征 | 🟠 中 | 只对本次新增的帧算 gaps：记录上次时间戳集合，`新帧 = ts > last_max_ts` 的条目；FPS 仍用全缓冲 span（保持其稳定性）。顺便把计算量降 3/4 |
| 1.2 | **`STALE_NS = 1_500_000_000` 是死代码**（`fps.py:44` 定义后全文无引用）——docstring 第 5 条宣称的"最新帧距今 >1.5s 归零"实际没实现，静止归零靠的是 advancing 判断 | 🟡 低 | 删除常量或补实现。advancing 方案本身没问题，但两者语义不同（advancing 对"首样本后缓冲未变"才归零），建议注释与代码对齐 |
| 1.3 | gfx 通道切换时执行 `gfxinfo <pkg> reset`（`fps.py:166`）——会清掉系统级统计，若用户同时开着 PerfDog 官方等工具会互相干扰 | 🟡 低 | 可接受（自研单机场景），文档注明即可 |

### 2. 帧时间 P50/P95/Max ——计算方式正确，口径有两处不统一

- 排序取分位、`gaps = 相邻 actualPresentTime 差`——方法正确；实测 5168 个样本 **P50≤P95≤Max 零违反**，P50 均值 16.8ms 与 60Hz 吻合；
- 🟡 **前后端 P95 定义不一致**：后端 `int(n*0.95)`（fps.py:319）vs 前端统计栏 `ceil(n*0.95)-1`（app.js:471）。n=127 时差 1 个位次，影响微小但报告两处数字可能对不上，建议统一；
- 继承 1.1 的缓冲残留问题（Max 均值 65ms 但峰值 3608ms 反复出现）。

### 3. CPU ——算法正确，3 个边界点

实测：整机 5.2–89.4%（无 >100，公式正确）；进程 0–208.9%（多核折算的正常表现）。

| # | 问题 | 严重度 | 建议 |
|---|---|---|---|
| 3.1 | **多核口径未在产品层说明**：`cpu_proc_pct` 是单核折算（可 >100%），与整机%（≤100%）画在同一张图同一 Y 轴，测试同学容易误读 | 🟡 低 | 图例注明"进程%为单核折算（多核可>100）"，或提供"÷核数"选项 |
| 3.2 | busy 计算未剔除 guest 时间：`/proc/stat` 的 guest/guest_nice 已含在 user/nice 内，`total=sum(fields)` 会重复计入（Android 上 guest 恒 0，实际无影响，但公式不严谨） | 🟡 低 | `busy = user+nice+system+irq+softirq+steal`，或 total 减 guest |
| 3.3 | 进程重启后首个差值为负被 `dproc >= 0` 拦掉——处理正确，但未跟踪 pid 变化，重启后第一个有效样本的 dt 横跨了进程死亡期，数值略失真 | 🟡 低 | result 里已有 pid，比较 `pid != last_pid` 时重置 `_last_*` |
| — | CLK_TCK 动态校准（`getconf` + 合理区间兜底 100）、`rfind(")")` 容忍进程名含空格——都是对的 | ✅ | 保持 |

### 4. 内存 ——发现本次评估最严重的数据问题

实测 5700 个双值样本中 **4140 个（72.6%）出现 RSS < PSS**，中位数 PSS 比 RSS 大 69MB、比值中位 1.165、最大 1.57。**这在物理上不可能**（单进程 PSS = 私有页 + 共享页按比例分摊，恒 ≤ RSS），说明是**系统性解析问题**而非测量噪声。

| # | 问题 | 严重度 | 建议 |
|---|---|---|---|
| 4.1 | **PSS 数值来源可疑**：两个正则（`TOTAL\s+PSS:` 大小写敏感 / `TOTAL\s+(\d+)\s+kB`）对 Android 14 的 dumpsys meminfo 输出匹配位置存疑——新版主表 TOTAL 行不带 "kB"，App Summary 是 "Total PSS:"（大小写不符），实际命中的可能是别的段落或旧格式行 | 🔴 高 | 在荣耀 ADT-AN00 上人工跑一次 `dumpsys meminfo <pid>` 对照正则命中行；正则改大小写不敏感并锚定 App Summary 段的 `Total PSS:` / `Total RSS:`；**PSS 与 RSS 改从同一份输出解析**（现在 PSS 来自 dumpsys、RSS 来自 /proc/status，双源不同步也放大偏差） |
| 4.2 | dumpsys meminfo 单次 0.5–1s 拖慢采样 | 🟠 中 | 见性能节第 2 条：smaps_rollup 一次拿到 Pss+Rss，同源且毫秒级 |

### 5. 网络 ——增量换算正确，一个认知错误

- `/proc/pid/net/dev` 差值 ÷ dt、排除 lo、多接口聚合、负差值钳 0——实现正确；实测下行峰值 31MB/s（游戏资源下载突发）合理；
- 🟡 **注释认知错误**（`network.py:6`）："该文件是进程的 net namespace 视图"——Android 上普通 App 与 shell 共享 root netns，**这实际是整机流量**，不是进程流量。对"测游戏耗流"的场景会高估（含后台应用流量）。这是 adb 免 root 方案的固有局限，PerfDog 官方免 root 模式同理。建议：注释改诚实；报告口径标注"整机（除 lo）"；若真要进程级需 root 读 `/proc/<pid>/net/xt_qtaguid` 或 NetworkStatsManager。

### 6. 温度/功耗 ——发现真实数据污染

实测 6 个采样点 **temp_c = 370.0**（电压 4.11V 正常）——电池 370°C 显然荒谬，反推原始值 3700：部分机型/状态下温度单位不是假设的 0.1°C 而是 0.01°C（或 dumpsys 返回毫摄氏度），代码无条件 `/10`。

| # | 问题 | 严重度 | 建议 |
|---|---|---|---|
| 6.1 | **温度无合理性校验**，单位假设被违反时直接产出 370°C 脏数据进报告 | 🟠 中 | 加物理范围校验：原始值换算后 >60°C（电池不可能）→ 尝试按 ×0.01 口径重算，仍异常则置 None 并记 error。一行防线避免整条曲线被污染 |
| 6.2 | 双通道降级 + `sys_unavailable` 探测缓存设计合理（省 3 次无效往返）；电流 abs() 处理符号差异、V×I 算功率——正确 | ✅ | 保持。小建议：`sys_unavailable` 一旦锁定永不重试，可加低频（如 10min）重探 |

---

## 总体评分：7.2 / 10

| 维度 | 分数 | 说明 |
|---|---|---|
| 架构与模块化 | 8.5 | 采集/解析/展示分层清晰，双通道与热切换设计成熟 |
| 代码风格与文档 | 8.5 | 注释写"为什么"且带真机实测记录，同类个人项目顶级水平 |
| 健壮性 | 7 | 异常兜底全面、并发锁到位；缺断连恢复与零测试 |
| 数据正确性 | 6 | 两个实证脏数据源（温度 370°C、PSS/RSS 倒挂 72.6%）+ 事件对齐存疑 |
| 性能 | 7 | 该省的 adb 往返大多省了，但 /api/runs 全量读盘是硬伤 |
| 测试 | 2 | 零测试，解析器这种高危代码裸奔 |

## 优先处理 TOP 5

1. **🔴 内存 PSS 解析核查**（指标 4.1）——主力指标系统性失真，先在真机上人工对照一次 dumpsys meminfo 输出修正正则，顺手改 smaps_rollup 双收益；
2. **🟠 温度物理范围校验**（指标 6.1）——已产生 370°C 脏数据，几行代码的防线；
3. **🟠 /api/runs 按 mtime 缓存行数**（性能 1）——output 越大越卡，随使用时间必然恶化；
4. **🟠 FPS 百分位/Jank 只算新帧**（指标 1.1）——消除卡死帧对后续 30–60s 数据的连带污染；
5. **🟠 前端两处正确性**（质量 2、3）——`hasData` 改 `!= null`（防全 0 数据卡片消失）+ 验证并修复事件标注线对齐。

**一句话总结**：工程底子扎实、文档意识出色，当前最大的风险不在代码结构而在**两个已实测存在的数据正确性缺陷**（内存口径、温度单位）——修掉它们并补上解析器 golden test，这个工具的可信度会从"参考级"升到"可出结论级"。
