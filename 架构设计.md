# 自研 PerfDog 工具 — 架构设计

> 项目：微信小游戏 / 安卓 App 真机性能采集
> 版本：V1.0（2026-09-11，对齐 HEAD de92c19 / 前端 v58）
> 状态：现行文档。V0.1（2026-08-12 评审稿）已被本文取代；指标口径细节以 `指标说明.md` 为准，
> 机型适配差异以 `devices.md` 为准。

---

## 1. 目标与定位

| 项 | 内容 |
|---|---|
| 目标 | 自研类 PerfDog 性能采集工具，**报告不传云端**，本地采集 → 本地看板 → 本地 HTML 报告 |
| 适用对象 | **微信小游戏**（主力，跑在 `com.tencent.mm:appbrandN` 子进程）+ **任意安卓 App**（通用扩展） |
| 采集指标 | FPS / Jank / 帧时间分布、CPU（整机/进程）、内存（PSS/RSS）、网络（整机上下行）、温度/电压/功耗 |
| 辅助能力 | logcat 事件标注层、设备信息记录、数据健全性自检、被测目标热切换 |
| 运行形态 | Windows + USB 安卓真机；纯 Python 标准库（uv 零安装或 PyInstaller 单文件 exe） |
| 已验证机型 | 荣耀 ADT-AN00（Magic3 Pro）、OPPO（ded7a388）——差异见 `devices.md` |
| 与微信开发者工具的关系 | 开发者工具 = 开发排查（代码层根因）；本工具 = 测试验收/回归（真机发布环境 + 本地报告留存） |

---

## 2. 系统架构

```
┌─────────────────────────── Windows 测试机 ───────────────────────────┐
│ collector/（Python 纯标准库）                                        │
│                                                                      │
│  main.py（编排）                                                     │
│   ├─ 启动：Adb 探测 → PidResolver 解析 → probe_cores /              │
│   │   probe_device_info → 写 jsonl meta 行                           │
│   ├─ 5 个采样线程（daemon，各自间隔，写 latest 快照）                │
│   │    fps 0.5s │ cpu 1s │ net 1s │ mem 2s │ therm 2s                │
│   ├─ 主线程（1s）：取快照落盘 jsonl + 推 web + 断连诊断              │
│   │   + data_health.check_rows_live 实时自检 + logcat 事件落盘       │
│   └─ 停止（Ctrl+C / POST /api/stop 同一路径）：停 logcat →           │
│       export_report 自动生成自包含 HTML → 看板继续服役               │
│                                                                      │
│  metrics/      fps.py cpu.py mem.py network.py thermal.py            │
│  支撑模块      adb.py pidresolver.py apk_label.py device_info.py     │
│                data_health.py logcat.py                              │
│  服务/导出     web.py（REST + SSE） export_report.py dashboard.py    │
│                                                                      │
│  web/          index.html（实时看板） report.html（历史报告）        │
│                assets/app.js（PerfCharts） echarts.min.js（本地化）  │
│                         │ USB / adb shell                            │
└─────────────────────────┼────────────────────────────────────────────┘
                ┌─────────▼─────────┐
                │ 安卓真机           │
                │ 微信 :appbrandN    │
                │ / 任意 App 进程    │
                └───────────────────┘
```

**离线形态**：`dashboard.py` 只起 web.py 看历史报告（无 adb、无采集），对应 `start_dashboard.bat`。
**打包形态**：GitHub Actions（`.github/workflows/build.yml`，tag `v*` 触发）用 PyInstaller
按 `perfdog.spec`（v57 起入库）构建单文件 exe 并发 Release；exe 启动后行为与
`python main.py --web` 一致（服务就绪后 `webbrowser` 自动开看板）。

---

## 3. 模块职责

### 3.1 采集编排 — `collector/main.py`

- **五采样线程并行**：`SAMPLER_INTERVALS = {fps:0.5, cpu:1.0, mem:2.0, net:1.0, therm:2.0}`，
  各自写 `latest` 快照（加锁）；慢指标（mem/therm 的 dumpsys ~0.5s）不拖快指标。
- **主线程 1s**：取最新快照组成一行落盘 `output/<时间戳>/perfdog_<时间戳>.jsonl`，
  同步 `web.add_sample(row)` 推看板。**注意**：fps 0.5s 采样但 1s 落盘，jsonl 为 1s 粒度。
- **启动探测**：`CpuCollector.probe_cores`（核数，`/sys/.../cpu/online` 优先）、
  `probe_device_info`（型号/平台/分辨率，一次往返）→ 写入 status 与 **jsonl 首行 meta**
  （`{"event":"meta", cores, proc_name, device}`），历史报告不依赖当前是否连设备。
- **断连诊断**：一轮 ≥4/5 指标带 error 累计 10 轮 → `adb.is_device_alive()` 探活并醒目告警，
  恢复自动继续。
- **实时自检**：每轮 `data_health.check_rows_live`（RSS<PSS / 采错进程线索，连续命中才告警）。
- **热切换**：看板 `/api/switch-target` → 重建绑定目标进程的采集器（fps/cpu/mem/net），
  jsonl 写 `target_switch` 事件行，config 持久化。
- **停止路径唯一**：Ctrl+C 第一次 / `POST /api/stop` 都置同一 `stop` 标志 → 退循环 →
  停 logcat → 自动生成 HTML 报告 → 看板继续；第二次 Ctrl+C 或 `/api/shutdown` 退进程。

### 3.2 指标采集器 — `collector/metrics/`

| 模块 | 数据源与算法要点 |
|---|---|
| `fps.py` | **sf 通道**：`dumpsys SurfaceFlinger --list` 匹配渲染层（层名保留 `#id`，荣耀实测必须；BLAST > 普通 SurfaceView > 窗口层兜底，跳过输入层 `ActivityRecordInputSink`）→ `--latency <layer>` 读帧时间戳（128 槽滚动缓冲，过滤 0 与 INT64_MAX 哨兵）。**FPS = 主段法**：相邻帧间隔 >0.5s 切段，取帧数最多主段 `(帧数−1)/主段跨度`（OPPO 稀疏缓冲修复）。**Jank/帧时间只算新增帧**（ts > 上次缓冲最大值），消除滚动缓冲重复污染。**Jank 阈值节奏校准**：新帧 ≥8 时帧间隔中位数吸附标准 vsync 档（60/90/120/144Hz，10% 容差），阈值 = 2×节奏×1.1；不足回退 2×refresh_ns。**gfx 通道**：普通 View 应用无 SF 帧统计时切 `dumpsys gfxinfo` 增量（连续 3 次 0 帧回退 sf；`_ever_surfaceview` 防 WebGL 应用误切后 FPS 永久归零）。层切换重置帧统计基准 |
| `cpu.py` | 整机 `/proc/stat` + 进程 `/proc/<pid>/stat` **一次合并 shell**（`__PDSEP__` 分隔）双采样点差值；进程% 单核折算（可 >100%）；`CLK_TCK` 动态校准；pid 变化时进程基线重置（整机基线保留） |
| `mem.py` | 优先 `/proc/<pid>/smaps_rollup`（毫秒级、PSS/RSS 同源）；失败回退 `dumpsys meminfo` 全输出搜 `TOTAL PSS`/`TOTAL RSS`（同源，兼容荣耀 App Summary 段内空行）；2s 线程级节流 |
| `network.py` | `/proc/<pid>/net/dev` 双采样点差值（除 lo）。⚠️ 整机口径（net ns 共享），非进程级 |
| `thermal.py` | `/sys/class/power_supply/battery/*` 优先（荣耀需 root → 锁定降级），回退 `dumpsys battery`；温度 >60°C 物理校验（按 0.01°C 口径重算，仍异常置 None） |

### 3.3 支撑模块 — `collector/`

| 模块 | 职责 |
|---|---|
| `adb.py` | adb 多路径探测（`C:\platform-tools` 优先，`adb version` 验证可用性）；shell 参数统一单引号包裹（层名含 `[ ] ( )`）；设备在线自检与 `is_device_alive()` 探活 |
| `pidresolver.py` | 单次 `ps -A -o PID,ARGS` 拿全进程；按 `process_pattern` 匹配子进程（多 appbrand 并存时**选累计 CPU 时间最大者**，防采到闲置驻留进程）；`current_pid()` 每 5s 校验 comm 防 pid 复用；失败 5s 节流；`proc_name` 写入 meta 行 |
| `apk_label.py` | 无 aapt 时纯 Python 解析 APK 的 AXML/resources.arsc 取应用名（设备 unzip 流式提取 + 看门狗超时 + 大小上限的内存安全设计） |
| `device_info.py` | 启动一次往返探测型号/市场名（内置型号映射表兜底）/平台/CPU/分辨率，写入 status 与 meta 行 |
| `data_health.py` | 数据健全性自检 5 规则（wrong_process / fake_jank / pss_jump / missing_metric / rss_lt_pss）——纯函数、阈值经 4 份真实数据调校；实时入口 `check_rows_live` + 报告级 `scan_rows` + `health_summary`（详见 指标说明.md「数据健全性自检」） |
| `logcat.py` | 零侵入捞小游戏 console.log（tag 白名单 + 关键词 + 同级别限流），设备时间锚点对齐 `t_ms`，断流自动重连；产物 `<name>.events.jsonl` 供看板/导出叠加事件标注层 |

### 3.4 服务与导出

**`web.py`（ThreadingHTTPServer，127.0.0.1）**：
- 页面：`/`（实时看板）、`/report.html`（历史报告）、`/assets/*`（basename 防穿越）
- 数据：`/api/status` `/api/target` `/api/device-apps`（60s 包列表缓存 + 扫描锁 + label 懒解析/失败 24h 重试 + 原子写缓存）`/api/latest`（300 点环形缓冲）`/api/stream`（SSE，`_seq` 游标增量）`/api/runs`（行数按 mtime/size 缓存）`/api/report`（按 mtime/size 缓存 + LRU 上限）`/api/events` `/api/raw`（打开 output 下已生成 HTML）
- 操作：`POST /api/rename`（sidecar 备注）`/api/switch-target` `/api/stop` `/api/shutdown`
- 健壮性：`_send_json_api` 统一兜底（handler 异常收敛为 200+`{"error"}`，防连接重置）、缓存读写加锁、label 解析可中止

**`export_report.py`**：jsonl → HTML（自包含：内联 echarts + app.js + 数据 + 事件层 + 健康告警横幅）/ CSV / XLSX；`flatten` 含 `fps_source` 列区分 sf/gfxinfo 通道；采集结束由 main.py 自动调用生成 HTML。

**`dashboard.py`**：离线看板入口（无设备，只看历史报告）。

### 3.5 前端 — `web/`（当前 ?v=57）

- `index.html`：实时看板。SSE 优先、断线降级 1s 轮询；被测应用下拉（就地 diff 更新防"左右横跳"，固定宽度）；停止采集按钮；设备/进程/数据目录常驻状态栏。
- `report.html`：历史报告。左侧记录列表（搜索/日期筛选/备注/对比选择，签名检测无感刷新）；右侧 meta（备注+被测+设备分行）、KPI 汇总卡（语义化分级 + 阈值着色）、6 张指标图、全局时间条、URL hash 定位、深色 toast、双报告对比面板、锁定时刻全指标快照条。
- `assets/app.js`（PerfCharts）：图表创建/渲染/统计、卡片拖拽排序（localStorage 记忆）、悬停贯穿线（echarts.connect）、点击锁定蓝线+数据浮层（DOM 覆盖层，legend 区域命中拦截）、自写时间拖动条（支持全局单条 + pointer events 触屏）、事件标注层（二分映射最近采样点）、`prepareRows` 清洗 event 行。
- `assets/echarts.min.js`：ECharts 5.5.0 本地化，不依赖外网 CDN。

### 3.6 测试与 CI

- `tests/`：`test_parsers.py`（fps/mem/cpu/thermal/pidresolver 解析器 golden test，含荣耀真机格式）、`test_data_health.py`（5 规则 + 真实数据回归）、`test_device_info.py`、`test_nearest_cat.js`（bun，前端纯函数断言）。当前 **76 条 Python 用例全绿**（`python -m unittest discover -s tests`）。
- CI：`.github/workflows/build.yml` —— tag `v*` 或手动触发，staging 复制 collector 源码后按 `perfdog.spec` 构建 exe 发 Release。

### 3.7 埋点 SDK（骨架，未联调）— `sdk/perfdog-sdk.js`

LayaAir 版小游戏内嵌埋点骨架：挂钩 `Laya.Stat`（多级降级 + 索引表兜底）读 DrawCall/三角面/GPU 内存，
rAF 包裹采样 JS 主线程忙占比，`scene()/mark()` 打点，`t_ms` 与采集器对齐，写 `wx.env.USER_DATA_PATH`。
待真机验证 release 版统计项可用性后联调（数据按 `t_ms` 与系统级数据合并）。

---

## 4. 数据格式

`collector/output/<YYYYmmdd_HHMMSS>/` 每次采集一个文件夹：

| 文件 | 内容 |
|---|---|
| `perfdog_<时间戳>.jsonl` | 首行 meta 事件（cores/proc_name/device），其后每行一个采样点（`ts/t_ms/target/fps/cpu/mem/net/therm`），热切换处插 `target_switch` 事件行 |
| `perfdog_<时间戳>.events.jsonl` | logcat 事件标注（`t_ms/tag/level/text`），仅微信小游戏目标时产生 |
| `perfdog_<时间戳>.html` | 自包含报告（采集结束自动生成，含健康告警横幅） |
| `*.remark.txt` | 看板备注 sidecar（不改原始 jsonl） |

采样点示例与字段含义见 `README.md` 与 `指标说明.md`。

---

## 5. 关键设计决策（历史沉淀，勿轻易回退）

1. **FPS 走 SurfaceFlinger 而非 gfxinfo**：微信小游戏 WebGL 走 SurfaceView，gfxinfo 恒 0 帧（已实测）。
2. **层名保留 `#id`**：荣耀 Android 14 去掉 #id 读不到帧；#id 变化靠失效重匹配应对。
3. **span/主段法算 FPS**：滚动缓冲下增量法恒 0；OPPO 稀疏缓冲需先按 0.5s gap 切段取主段。
4. **Jank/帧时间只算新增帧**：128 槽窗口 > 采样间隔，全缓冲计算会被一次卡顿污染 30~60s。
5. **Jank 阈值跟"实际呈现节奏"而非面板 vsync**：面板高刷 + 游戏锁帧场景假 Jank 70~100%（2026-08-27 归因）。
6. **进程选择按活跃度（累计 CPU）而非 pid 顺序**：多 appbrand 并存时 pid 最小的常是闲置预加载进程（2026-08-27 采错进程事故）。
7. **PSS/RSS 同源解析**：双源（dumpsys + /proc/status）实测 72.6% 样本 RSS<PSS 倒挂。
8. **停止路径唯一**：Ctrl+C 与看板停止按钮复用同一 stop 标志，行为一致可预期。

---

## 6. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| 一 | 采集器骨架（FPS/CPU/内存） | ✅ 2026-08-12 |
| 二 | Web 看板 + 导出 + 埋点 SDK 骨架 | ✅ 2026-08-14（SDK 待联调） |
| 三 | 网络/温度 + 任意 App + 看板热切换 | ✅ 2026-08-20 |
| 四 | 数据正确性攻坚（同源内存/新增帧 Jank/双通道防呆/进程重启基线）+ 打包 exe | ✅ 2026-08-21~26 |
| 五 | 多机型适配（OPPO）+ 数据健全性自检 + 设备信息 + 多进程活跃选择 + Jank 节奏校准 | ✅ 2026-08-27~09-01 |
| 六 | 前端体验（KPI 分级/全局时间条/对比/快照条/toast/hash）+ CI 打包 | ✅ v48~v57 |
| 下一步 | 埋点 SDK 真机联调；换机验收扩充 devices.md；长测（30min+）验证 | 待排期 |

---

*本文档描述当前实现（HEAD de92c19）。指标口径细则见 `指标说明.md`；机型差异见 `devices.md`；
历史评估报告（kimi/qwen 双模型评审）为归档文档，结论已被各轮修复吸收。*
