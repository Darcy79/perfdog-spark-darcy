# 自研 PerfDog 工具 — 架构设计（第一阶段）

> 项目：《火线战场》微信小游戏性能测试
> 阶段：第一阶段（架构文档 + 采集器骨架：FPS/CPU/内存）
> 日期：2026-08-12
> 状态：V0.1 评审稿

---

## 1. 目标与定位

| 项 | 内容 |
|---|---|
| 目标 | 自研一款类 PerfDog 的性能采集工具，**报告不传云端**，从本地采集数据生成本地 HTML 报告 |
| 适用对象 | **微信小游戏**（当前主力）+ **安卓原生 App**（通用扩展，见 §3.3） |
| 采集范围（第一阶段） | 安卓单端 · ADB 系统级采集：FPS、CPU、内存 |
| 采集范围（增强版预留） | 网络、功耗、温度、帧时间分布（framestats 逐帧） |
| 埋点范围（待引擎确认后设计） | 小游戏内嵌 SDK：rAF 帧率 / JS 线程 / 场景打点 |
| 报告 | 本地 HTML（ECharts，不传云端）——第二阶段 |
| 与微信开发者工具的关系 | 开发者工具 = 开发排查（代码层根因）；本工具 = 测试验收/回归（真机发布环境 + 本地报告留存） |

**组合拳**：自研埋点标卡顿时间段 → 开发者工具查代码层根因。

---

## 2. 系统架构

```
┌──────────────────────────── Windows 测试机 ────────────────────────────┐
│                                                                       │
│   collector/（Python，标准库，免依赖）                                  │
│   ┌──────────┐   ┌──────────┐   ┌──────────┐                          │
│   │ FPS 采集  │   │ CPU 采集  │   │ 内存采集  │                          │
│   │ gfxinfo  │   │ /proc/*  │   │ meminfo  │                          │
│   └────┬─────┘   └────┬─────┘   └────┬─────┘                          │
│        └──────────────┼──────────────┘                                │
│                    ┌───▼────┐   采样循环（间隔可配）                    │
│                    │ main.py│──► JSONL 落盘 output/*.jsonl            │
│                    └───┬────┘                                         │
│                        │ ADB 桥（adb.py）                              │
└────────────────────────┼──────────────────────────────────────────────┘
                         │ USB
              ┌──────────▼──────────┐
              │  安卓真机（微信小游戏） │
              │  com.tencent.mm      │
              │  :appbrandN 子进程    │
              └─────────────────────┘
```

**采集链路（第一阶段）**：`adb shell dumpsys gfxinfo <pkg>` / `adb shell cat /proc/<pid>/stat` / `adb shell dumpsys meminfo <pid>` → 指标计算 → 带时间戳 JSONL 落盘。

**埋点链路（第二阶段，引擎确认后）**：小游戏内嵌 SDK 采集 rAF 帧率 / JS 线程 / 场景打点 → 经微信开放数据上报或本地日志导出 → 与系统级数据按时间轴合并。

---

## 3. 指标采集方案（第一阶段）

> 共性前提：Windows + USB 连接安卓真机；需开启 USB 调试；无需 Root（本表全部免 Root 可读）。
> 目标进程：微信小游戏运行在微信 App 的 `com.tencent.mm:appbrandN` 子进程（Android 侧 Chromium 运行时）。

| 指标 | ADB 命令/数据源 | 口径 | 版本/权限要求 |
|---|---|---|---|
| FPS / Jank 率 | **`dumpsys SurfaceFlinger --list` 匹配小游戏 SurfaceView 渲染层（保留 `#id`，荣耀/Android14 实测去掉 #id 读不到）→ 周期 `dumpsys SurfaceFlinger --latency <layer>` 读帧时间戳**（gfxinfo 对微信小游戏 WebGL/SurfaceView 采不到帧，已废弃） | **FPS = (帧数−1) ÷ 缓冲内首末帧时间戳跨度**（span 法，对 128 槽滚动缓冲稳定）；**Jank 率 = 帧间隔 > 2×刷新周期 占比**（自适应：60Hz→33.3ms、120Hz→16.7ms）；哨兵值（INT64_MAX）过滤；静止判断 = 最新帧时间戳未推进 | 免 Root；游戏在前台时渲染层存在，退后台销毁（自动重匹配） |
| CPU 整机占用 | `cat /proc/stat`（cpu 行，双采样点差值） | busy/(busy+idle) delta | 全部 Android，免 Root |
| CPU 进程占用 | `cat /proc/<pid>/stat`（utime+stime，双采样点差值） | 进程 CPU% = (utime+stime delta)/CLK_TCK/墙钟间隔 | 全部 Android，免 Root |
| 内存（PSS） | `dumpsys meminfo <pid>` 解析 `TOTAL PSS` | PSS（含共享内存按比例分摊，与 PerfDog 内存口径一致） | 免 Root |
| 内存（RSS 兜底） | `cat /proc/<pid>/status` 的 `VmRSS` | RSS | 免 Root |

### 3.1 增强版预留指标（第二阶段）

| 指标 | 数据源 | 备注 |
|---|---|---|
| 帧时间分布（P50/P95/Jank 阈值） | `dumpsys gfxinfo <pkg> framestats`（需 `setprop debug.hwui.profile true`） | user 版多数机型受限，降级为 3.1 汇总口径 |
| 网络 | `/proc/<pid>/net/dev` 或 `dumpsys netstats` | 需按进程过滤，第二期评估 |
| 功耗 | `cat /sys/class/power_supply/battery/...`（current_now/voltage_now/temp） | 多数机型可读，机型差异需适配表 |
| 温度 | 同上 battery/temp 或 therm 节点 | 同上 |

### 3.2 口径注意事项
- **FPS 采集**：SurfaceFlinger `--latency` 输出每行三列（desired/actual/ready 时间戳，ns），取第 2 列；滚动缓冲约 128 槽。**必须带 `#id` 完整层名**（荣耀 ADT-AN00/Android 14 实测去掉 #id 读不到；一般工具都去 #id，此设备例外，故保留并靠失效重匹配应对 #id 变化）。**FPS 用 span 法**（缓冲内首末帧时间戳跨度）而非增量法——增量法在缓冲满时恒 0。**哨兵值过滤**：缓冲末位常驻 `INT64_MAX`（9223372036854775807），需丢弃否则 span 算成天文数字。**静止判断**：帧时间戳与 uptime 基准不同（不能直接比），改用"缓冲最新帧时间戳是否推进"——推进=在渲染，未推进=静止/暂停 → FPS 归零。**Jank 阈值 = 2×刷新周期**（自适应刷新率，PerfDog 口径）。**优先 `(BLAST)` 层**（Android 12+ 新式缓冲提交）。游戏退后台渲染层销毁 → 报 `no_layer` 并自动重匹配，回前台恢复。
- **CPU 采样**：`/proc/stat` 与 `/proc/<pid>/stat` 需**同一次采样点**成对读取，避免跨点错位；两采样点间隔 ≥ 1s 误差较小。
- **进程选择**：微信多进程（主进程 + appbrandN），小游戏逻辑在 `appbrand` 子进程；采集器按配置的 `process_pattern` 优先匹配，匹配失败回退主进程。
- **Android 版本差异**：`pidof` 需 Android 7.0+；老版本降级用 `ps -A | grep`（骨架标注待补）。
- **adb shell 参数引号**：layer 名含 `[ ] ( )` 等 shell 特殊字符，`adb.shell()` 统一单引号包裹参数再发设备端（adb.py 已处理）。

### 3.3 支持范围扩展：安卓原生 App（2026-08-12 用户需求确认）

本采集器**天然支持安卓原生 App**——ADB 采集针对设备上任意应用进程，微信小游戏只是"微信 App 子进程"的特殊场景。测原生 App 只需改配置：

| 场景 | package | process_pattern | 说明 |
|---|---|---|---|
| 微信小游戏 | `com.tencent.mm` | `appbrand` | 小游戏跑在微信 `:appbrandN` 子进程 |
| 安卓原生 App | 目标 App 包名（如 `com.example.game`） | 留空 `""` | 直接 `pidof <pkg>` 命中主进程 |

**原生 App 的指标差异**：

| 指标 | 与微信小游戏的差异 | 兼容性 |
|---|---|---|
| FPS | `dumpsys gfxinfo <pkg>` 同样适用 | Android 12+ 部分机型要求 App 声明 `<profileable android:shell="true"/>` 或 debuggable，否则可能拿不到逐帧数据（只出汇总）——骨架已兜底降级 |
| CPU | `/proc/<pid>/stat` 无差异 | ✅ 全兼容 |
| 内存 | `dumpsys meminfo <pkg>` 无差异 | ✅ 全兼容 |
| 埋点 SDK | 原生 App **不需要**埋点（ADB 已覆盖），除非要引擎级指标 | 免侵入 |

**使用方式**：复制 `config.json` 为 `config.app.json`，改 `package` 为目标包名、`process_pattern` 为 `""`，运行 `python main.py --config config.app.json`。详见 README「测普通安卓 App」。

---

## 4. 数据格式（JSONL 落盘）

`output/perfdog_<YYYYmmdd_HHMMSS>.jsonl`，每行一个采样点：

```json
{
  "ts": 1723412345.678,
  "t_ms": 1234.5,
  "fps": {"total_frames": 60, "janky_frames": 3, "fps": 60.0, "jank_rate": 0.05},
  "cpu": {"pid": 12345, "cpu_total_pct": 12.3, "cpu_proc_pct": 8.5},
  "mem": {"pid": 12345, "pss_kb": 234567, "vmrss_kb": 345678}
}
```

字段约定：
- `ts`：Unix 秒（float）；`t_ms`：相对启动毫秒，便于对齐埋点时间轴。
- 指标采集失败时对应键为 `null` 或带 `*_error` 说明，**不中断整轮采集**。

---

## 5. 埋点 SDK 接口设计（Laya 引擎版，2026-08-12 引擎已确认）

**引擎结论**：《火线战场》用 **LayaAir 3.4 引擎**，导出微信小游戏后渲染技术栈为 **WebGL**（非 Cocos、非 Unity WebGL）。运行于微信小游戏运行时（Android 侧 Chromium + 适配层 Adapter）。

### 5.1 Laya 内置性能统计 API（重大利好，字段已按 LayaAir 3.4 源码确认）

LayaAir 引擎自带 `Laya.Stat` 性能统计类。**已按 LayaAir 官方源码确认**（`src/layaAir/laya/utils/Stat.ts`、`layagl/StatisticsContext.ts`；LayaAir_3.4 分支与 3.3 枚举/读取机制完全一致，SDK 索引表直接适用）：

**读取机制**：`Stat.FPS` 是稳定 getter；其余指标通过 `LayaGL.statAgent.getElementData(StatElement.xxx)` 读取，`StatElement` 为**顺序数字枚举**（非直接属性字段）。`Stat` 挂在全局 `window.Stat`。

| 读取方式 | 指标 | 价值 |
|---|---|---|
| `Laya.Stat.FPS` | 当前 FPS | 引擎内部帧率（与系统 gfxinfo 互证） |
| `Laya.Stat.show(x,y,views)` / `.hide()` | 游戏内性能面板 | 开发期人工验证 |
| `getElementData(CT_DrawCall)` | **DrawCall 渲染批次** | 卡顿根因高价值指标 |
| `getElementData(CT_Triangle)` | 三角面数 | 场景负载 |
| `getElementData(M_GPUMemory)` | GPU 内存（字节） | 显存压力 |
| `getElementData(T_Frame_Time)` | 帧耗时 | 与 FPS 互证 |
| `getElementData(CT_OpaqueDrawCall / CT_TransDrawCall / CT_ShadowDrawCall / CT_Instancing_DrawCall)` | 细分 DrawCall | 定位渲染瓶颈（不透明/透明/阴影/实例化） |
| `getElementData(C_Sprite2DCount / C_Sprite3DCount)` | 2D/3D 节点数 | 场景复杂度 |
| `getElementData(C_ShurikenParticleRenderCount)` | 粒子渲染数 | 粒子开销 |

**版本/裁剪风险**（SDK 已做多级降级 + 启动探测）：
- `Stat.show()` 默认展示元素受 `PlayerConfig.statEnum` 控制——**release 发布版统计项可能被裁剪**（真机验证点）。
- 枚举数字顺序随引擎版本可能变化：SDK 优先用枚举对象按名字访问（`Laya.StatElement` / `globalThis.StatElement`），失败才用内置索引表兜底。
- `StatElement` / `LayaGL` 若未暴露全局，引擎级指标降级为空，SDK 退化为 rAF 计数。

### 5.2 埋点 SDK 设计（perfdog-sdk.js）

Laya 小游戏入口（`js/main.js` / 场景脚本）注入，接口草案：

```js
window.PerfDogSDK = {
  start(config),            // 配置：采样间隔、场景开关
  scene(name),              // 场景打点：scene('战斗-甲修') —— 挂钩 Laya 场景切换
  mark(name),               // 关键节点：mark('修甲开始') mark('修甲完成')
  report(),                 // 导出/上报：本地日志（wx.getFileSystemManager 写临时文件）
};
```

采集项（三层）：
1. **引擎级**（挂钩 `Laya.Stat`）：FPS / DrawCall / 三角面数 / 节点数 —— 每采样周期读取
2. **JS 线程**：`setTimeout` 任务堆积计数、`requestAnimationFrame` 回调耗时（包一层计数测 delta）
3. **场景打点**：`scene()` / `mark()` 时间戳（毫秒），与系统级 `t_ms` 时间轴对齐

### 5.3 数据回传链路（两条路）

| 方案 | 做法 | 适用 |
|---|---|---|
| A. 本地日志导出（推荐，无侵入发布链路） | SDK 采样写 `wx.env.USER_DATA_PATH` 临时文件，测试后经微信开发者工具或 adb `run-as`/备份取出 | 测试验收/回归场景，不碰线上 |
| B. 内网回传 | SDK 通过 `wx.request` 上报内网服务器 | 需要实时看板时（增强版） |

时间戳约定：SDK 用 `Date.now()` 记录**相对启动偏移**，采集器落盘同样带 `t_ms`，报告阶段按 `t_ms` 对齐叠加"卡顿时间段 + 当时场景 + DrawCall"。此方案在微信小游戏上可行性高（可埋点），比原生 App 免侵入方案更优。

---

## 6. Web 看板（第二阶段，2026-08-12 用户确认形态）

**形态**：采集器本地起 HTTP 服务（127.0.0.1，Python 标准库，不传云端），浏览器打开即为 PerfDog 式看板。

| 页面 | 内容 |
|---|---|
| `http://localhost:8080` | **实时看板**：FPS/CPU/内存 ECharts 实时曲线（1s 轮询），设备/进程/运行状态栏，均值/P95/最低统计 |
| `http://localhost:8080/report.html` | **历史报告**：列出 `output/*.jsonl`，点开看完整曲线 + 统计 |

**实现**：
- `collector/web.py`：ThreadingHTTPServer，端点 `/api/status`、`/api/latest`（环形缓冲 300 点）、`/api/runs`、`/api/report?name=`；静态资源 `/assets/*`（ECharts 5.5.0 已本地化到 `web/assets/echarts.min.js`，不依赖外网 CDN）
- `main.py --web`：采集循环每点推入内存队列；Ctrl+C 停采集后 Web 服务继续（可看本次+历史），再按一次退出
- 路径穿越防护：静态文件与报告名均做 basename/白名单校验

**后续增强（二期）**：SSE 推送替代轮询、埋点 SDK 数据叠加（场景/卡顿标注层）、多指标同图对比、设备管理页。

---

## 7. 项目结构

```
自研perfdog/
├── 架构设计.md
├── README.md
└── collector/
    ├── config.json          # 目标包名/进程/采样配置
    ├── main.py              # 入口：采样调度 + 落盘 + 摘要
    ├── adb.py               # ADB 桥（设备选择/命令执行/自检）
    ├── pidresolver.py       # 目标进程解析（appbrand 匹配）
    └── metrics/
        ├── __init__.py
        ├── fps.py           # gfxinfo FPS/Jank
        ├── cpu.py           # /proc/stat + /proc/<pid>/stat
        └── mem.py           # dumpsys meminfo PSS + VmRSS
```

---

## 8. 风险与待办

| # | 风险/待办 | 影响 | 对策 |
|---|---|---|---|
| 1 | ~~引擎类型未确认~~ ✅ 已确认 = LayaAir（WebGL 渲染） | 已解除 | 埋点 SDK 按 §5 落地 |
| 1a | `Laya.Stat` 在微信小游戏 **release 版**统计项可能被裁剪 | 引擎级指标（DrawCall 等）缺失 | 真机验证后裁剪项降级为 rAF 计数兜底 |
| 2 | ~~gfxinfo 对小游戏采不到帧~~ ✅ 已改为 SurfaceFlinger（`--latency` 按渲染层读帧） | 已解除 | 游戏退后台无渲染层时自动重匹配 |
| 3 | 微信多进程 pid 漂移（小游戏重启进程号变化） | 进程级指标断档 | PidResolver 每轮重试解析 + 记录切换 |
| 4 | 低版本 Android 无 `pidof` | 进程解析失败 | 补 `ps` 降级路径（待办） |
| 5 | 低端机 `dumpsys meminfo` 执行慢（~0.5s） | 采样间隔抖动 | 内存采样独立节流（待办：间隔 ≥2s） |
| 6 | 多机型功耗/温度节点差异 | 增强版指标覆盖不足 | 机型适配表（第二期） |

---

## 9. 里程碑

| 阶段 | 内容 | 预计 |
|---|---|---|
| 一（本阶段） | 架构文档 + 采集器骨架（FPS/CPU/内存） | 交付即达 |
| 二 | Web 看板（实时曲线 + 历史报告，ECharts 本地化）+ 埋点 SDK 真机联调 | 1~2 周 |
| 三（增强版） | 网络/功耗/温度 + 多机型适配 + 测试报告模板 | 2~3 周 |
| （不建议） | iOS 支持 | 6 个月+ |

---

*本文件为评审稿，指标口径与技术选型可在真机验证后修订。*
