# 自研 PerfDog — 指标详解与使用说明

> 配套工具：`collector/main.py`（采集）、`web/`（看板）、`export_report.py`（导出）
> 版本：V0.3（2026-08-25）· 更新：v36~v39 口径修正——Jank/帧时间改按"新增帧"计算
> （消除滚动缓冲重复污染，见「一、2. Jank 率」下方读数说明）；PSS/RSS 改同源解析（smaps_rollup 优先，
> meminfo App Summary 段兼容真机空行）；温度增加物理范围校验；网络口径澄清为整机流量；
> gfxinfo 连续 0 帧自动回退 SurfaceFlinger（WebGL 应用 FPS 不再永久归零）；
> 渲染层重建后帧统计不再跨层残留；CSV/Excel 导出新增「FPS通道」列区分 sf / gfxinfo 口径
> （V0.2：2026-08-12，新增帧时间/网络/温度指标、导出与一键启动）

---

## 一、帧率类指标

### 1. FPS（帧率）
| 项 | 说明 |
|---|---|
| 含义 | 每秒渲染的帧数，衡量游戏流畅度最核心的指标 |
| 采集方式 | SurfaceFlinger 帧时间戳 **span 法**：(帧数−1) ÷ 缓冲内首末帧时间戳跨度（PerfDog 同思路） |
| 双通道 | 默认走 SurfaceFlinger（SurfaceView/小游戏）；普通 View 应用无 SF 帧统计时**自动切换** `dumpsys gfxinfo` 增量（切换时会执行一次 `gfxinfo <pkg> reset`）。⚠️ reset 会**清空系统级帧统计**——若同时开着其他依赖系统帧统计的监测工具（如 PerfDog 官方），其累计数据会被打断，避免并行使用即可 |
| 上限 | **采集器无上限**（90/120/144Hz 都能采）；**游戏帧率上限 = 手机屏幕刷新率**（60Hz 屏最多 60fps） |
| 参考值 | 满帧 = 屏幕刷新率（60Hz 屏 59.8~60）；低于 30 明显卡顿；稳定贴近满帧 = 流畅 |
| 判断 | FPS 低且 CPU 也低 → 渲染瓶颈或锁帧；FPS 低但 CPU 高 → 逻辑/脚本瓶颈 |

### 2. Jank 率（卡顿率）
| 项 | 说明 |
|---|---|
| 含义 | 卡顿帧占已渲染帧的比例。**帧间隔超过 2×屏幕刷新周期** 计为卡顿帧 |
| 自适应阈值 | 60Hz 屏 → 帧间隔 >33.3ms；90Hz → >22.2ms；120Hz → >16.7ms（随刷新率自动调整，与 PerfDog 口径一致） |
| 参考值 | <1% 优秀；1~5% 轻微（偶发卡顿）；>5% 明显（需排查） |
| 注意 | Jank 率统计的是已提交的帧；静止画面（不渲染）不计入，FPS=0 时该项无意义。⚠️ 两通道口径略有差异：SurfaceFlinger 通道按"帧间隔 >2×刷新周期"计算；gfxinfo 通道用系统自带的 Janky 计数（超帧预算），同一次测试切换通道前后数值可能有小跳变，属正常。导出的 CSV/Excel 有「FPS通道」列（sf / gfxinfo）可核对该采样点走的哪条通道 |

> **⚠️ 读数说明（v36 起，务必先读）：Jank 率与帧时间按"新增帧"计算——单点高 ≠ 全程卡。**
>
> SurfaceFlinger 的 `--latency` 是 128 槽滚动缓冲（约 2.13s 数据窗），比 0.5s 采样间隔长。
> 早期版本对**整个缓冲**算 Jank/帧时间，一次 2~3s 的卡死会在缓冲里留 30~60s，期间每个采样点
> 都被同一次卡顿反复污染。v36 起改为**只对本次新增的帧**（时间戳大于上次缓冲最大值）计算。
>
> 代价是单个采样点的分母变小：某个 0.5s 窗口里只新增了 3~8 帧时，其中一次大 gap 就会让该点
> 的 Jank 率算出 0.7~0.86 这种很高的值（如 6 帧中 5 个间隔有 4 个超阈值 = 0.8）。
> **这是分母小导致的统计噪声，不代表全程卡顿**。正确解读方式：
>
> - 看**均值 / 分位数**（看板统计栏、报告摘要卡片的"卡顿率"是全程聚合值），而不是单点峰值；
> - 看**是否连成持续段**：连续多个采样点都高 = 真卡顿；孤立单点尖峰 = 一次瞬时 gap；
> - 结合**帧时间 Max 与 FPS 曲线**交叉验证：真卡顿时 FPS 会同步塌陷，噪声点不会。
>
> 另：渲染层重建（切场景/游戏重启）后帧统计基准会重置，新层前 1~2 个采样点可能不出
> 帧时间读数（曲线短暂断点），属正常——避免沿用上一个场景的残留数值。
>
> **⚠️ 阈值节奏校准（2026-08-27，v45）：** Jank 阈值不再只跟 `--latency` 首行报告的
> 刷新周期走，而是按**实际帧间隔节奏**校准——面板支持 60/90/120/144Hz 切换时，若面板
> 跑在 120Hz 但游戏被平台锁 60fps，旧口径阈值 16.67ms 与 60fps 帧间隔 16.7ms 压线，
> 会误报 70~100% 的假 Jank（实测 8/27 数据 hz=120 段 Jank 78.6%、hz=60 段仅 3.9%）。
> 修复后：新帧 ≥8 时取帧间隔中位数吸附最近标准档（60/90/120/144Hz，10% 容差），
> 阈值 = 2×节奏×1.1；新帧不足回退刷新周期口径。注意：**稳态低帧游戏（如锁 30fps）
> 阈值会随自身节奏放宽**，与按面板口径的判定有偏差——主场景（微信锁 60fps）两口径一致。

### 3. 帧时间 FrameTime（P50 / P95 / Max，单位 ms）
| 项 | 说明 |
|---|---|
| 含义 | 相邻两帧的时间间隔分布。P50 = 中位数帧耗时；P95 = 95% 的帧都在该耗时内；Max = 最大帧耗时（最卡的那一帧） |
| 参考 | 60Hz 满帧每帧 16.7ms；**P95 超过 2×刷新周期**说明存在明显卡顿风险；Max 值远高于 P95 是瞬时掉帧（如加载、GC） |
| 用途 | 卡顿定位：P95 高 = 持续负载重；仅 Max 高 = 偶发尖峰（找当时场景/操作） |

### 4. refresh_hz（屏幕刷新率）
- 采集到的设备当前屏幕刷新率（Hz）。决定游戏帧率上限与 Jank 阈值。
- 高刷手机在系统"显示→屏幕刷新率"切到 90/120/144Hz 后，游戏帧率上限随之解锁。

---

## 二、CPU 类指标

### 5. CPU 整机占用（cpu_total_pct，单位 %）
| 项 | 说明 |
|---|---|
| 含义 | 设备**所有 CPU 核平均**占用率（0~100%），看游戏对整机资源的压力 |
| 数据源 | `/proc/stat` 双采样点差值（busy/total） |
| 参考 | 整机 50%+ 说明设备较忙；长期 80%+ 会发热降频 |

### 6. CPU 进程占用（cpu_proc_pct，单位 %）
| 项 | 说明 |
|---|---|
| 含义 | **目标进程**占用，**按单核折算**（100% = 占满 1 个核；**多核并行可 >100%，如 150% = 1.5 核**，属正常表现，非异常） |
| 数据源 | `/proc/<pid>/stat` 的 utime+stime 差值（CLK_TCK 动态校准） |
| 注意 | 8 核设备理论总上限 800%；游戏只用了部分核，通常 20~200% 区间波动 |
| 判断 | 接近单核 100%+ 说明有单线程热点；多核吃满说明并行负载重 |

> 小游戏进程 = 微信的 `com.tencent.mm:appbrandN` 子进程（WebGL/JS 逻辑所在），工具自动匹配。

---

## 三、内存类指标

### 7. PSS（按比例分摊占用，单位 KB/MB）⭐ 主口径
| 项 | 说明 |
|---|---|
| 含义 | **Proportional Set Size**：进程实际占用的物理内存，其中**共享内存按使用进程数比例分摊**。例如某 10MB 共享库被 2 个进程使用，各算 5MB |
| 数据源 | 优先 `/proc/<pid>/smaps_rollup`（Android 8+，毫秒级，PSS/RSS 同源）；失败回退 `dumpsys meminfo <pid>` App Summary 段的 `TOTAL PSS`（同源解析 RSS） |
| 为什么用它 | 最准确地反映"该进程对设备物理内存的真实占用贡献"，内存占用/泄漏评估的主流口径 |

### 8. RSS（驻留内存，单位 KB/MB）
| 项 | 说明 |
|---|---|
| 含义 | **Resident Set Size**：进程当前驻留在物理内存的量，**共享库被完整算入每个使用它的进程** |
| 与 PSS 区别 | RSS 会"重复计算"共享库 → 通常比 PSS 偏大（如微信多个子进程共享引擎库时差异明显） |
| 用途 | 作为辅助参考，评估物理驻留 |

### 9. PerfDog 的 "Memory" 到底是什么？
**PerfDog 看板上的"内存/Memory"指标，底层数值就是 PSS**（单位 MB），与我们采集的 PSS 是**同一口径**——只是显示名不同（PerfDog UI 写"内存"，数值 = PSS）。**结论：本工具的 PSS 与 PerfDog 内存可直接对比**，无需换算。

补充几个易混淆口径（PerfDog/工具里可能出现）：
| 口径 | 全称 | 含义 | 数值大小关系 |
|---|---|---|---|
| **PSS** | Proportional Set Size | 按比例分摊共享内存 | 中等（主口径） |
| **RSS** | Resident Set Size | 驻留物理内存（共享重复计） | 较大 |
| **USS** | Unique Set Size | 进程独占内存 | 最小 |
| **VSS** | Virtual Set Size | 虚拟内存（含未分配/映射） | 最大，基本无参考意义 |

### 10. 内存异常怎么看
- **持续上涨不回落** → 内存泄漏（重点场景：反复进出战斗/修甲界面，对比前后 PSS）
- 单次跳涨后回落 → 大资源加载（正常）
- 系统内存不足会被回收（PSS 骤降），小游戏易被系统杀进程 → 关注峰值与稳定性

---

## 四、网络类指标（V0.2 新增）

### 11. 上行/下行速率（KB/s）
| 项 | 说明 |
|---|---|
| 含义 | **整机网络收发速率**（每秒传输的字节数，不含本地回环 lo）。⚠️ 口径（2026-08-21 修正）：Android 普通 App 与 shell 共享 root netns，`/proc/<pid>/net/dev` 实际是**整机流量**而非进程级；前台小游戏占绝大部分流量时近似可用，微信主进程心跳/系统后台同步也会被计入。免 root 无严格进程级方案（PerfDog 官方免 root 同理） |
| 数据源 | `/proc/<pid>/net/dev` 双采样点差值（rx_bytes / tx_bytes） |
| 用途 | 定位"卡顿是不是网络问题"——延迟敏感玩法（战斗/修甲同步）下网络抖动会表现为卡顿；观察资源加载高峰流量 |
| 注意 | 聚合所有非 lo 接口；速率波动大属正常，看趋势与峰值 |

---

## 五、功耗温度类指标（V0.2 新增）

### 12. 电池温度（单位 °C）
| 项 | 说明 |
|---|---|
| 含义 | 电池温度（多数机型可读；部分机型需要 root 或厂商节点差异读不到）。⚠️ **是电池温度 ≠ SoC 温度**，更新慢且有 1°C 量化，实际 SoC 温度更高 |
| 数据源 | `/sys/class/power_supply/battery/temp`（单位 0.1°C）；降级 `dumpsys battery`（自动做物理范围校验：>60°C 按 0.01°C 口径重算，仍异常置 None） |
| 参考 | 35°C 以下正常；40°C+ 偏热；45°C+ 可能触发**降频**（表现为帧率骤降/卡顿） |
| 判断链 | 发热 → 降频 → 掉帧：若温度曲线上升的同时 FPS 下滑，多为热降频 |

### 13. 电流 / 电压 / 功率（单位 mA / V / W）
| 项 | 说明 |
|---|---|
| 含义 | 电池实时放电电流（负值表示充电）、电压、瞬时功率（= 电压×电流） |
| 数据源 | `/sys/class/power_supply/battery/current_now`（uA）、`voltage_now`（uV） |
| 用途 | 评估游戏功耗：同场景下功耗高 = 优化空间；功率曲线与帧率/场景关联 |
| 注意 | 充电时电流为负，工具会按绝对值展示功耗趋势 |

---

## 六、指标间关联与异常排查

| 现象 | 优先看 | 可能原因 |
|---|---|---|
| FPS 低 + CPU 进程高 | 帧时间 P95、Jank | 逻辑/脚本瓶颈（单线程热点） |
| FPS 低 + CPU 进程不高 | 帧时间 P95、refresh_hz | 渲染瓶颈（DrawCall/GPU，配合埋点看）、或锁帧 |
| Jank 高 | 帧时间 Max/P95 | 偶发尖峰：找当时场景（加载、GC、网络） |
| 温度高 + FPS 下滑 | 温度曲线 vs FPS 曲线 | 热降频 |
| 网络波动 + 战斗卡 | 网络速率曲线 | 弱网/请求阻塞 |
| PSS 持续上涨 | PSS 全程趋势 | 内存泄漏 |

---

## 七、测试场景建议

| 场景 | 关注指标 | 目的 |
|---|---|---|
| 主界面 / 大厅 | FPS、CPU、内存 | 基准基线 |
| 战斗（高负载） | FPS、Jank、帧时间 P95、温度 | 峰值负载下的流畅度与发热 |
| 修甲 / 玩法交互 | FPS、Jank | 交互响应是否卡顿 |
| 反复进出界面 | 内存 PSS | 泄漏检测 |
| 弱网（可配合网络限速） | 网络速率、Jank | 网络对体验的影响 |

**对比方法**：同场景固定操作路径采 3 次取平均/中位，两次版本对比用同一场景。

---

## 八、使用快捷入口

| 需求 | 命令/操作 |
|---|---|
| 一键启动采集 + 看板 | 双击 `start_perfdog.bat`（自动定位目录 + 打开浏览器） |
| 只查看历史报告 | 双击 `start_dashboard.bat`（无需手机，数据在本地 jsonl） |
| 手动启动 | `cd collector` → `uv run --no-project python main.py --web` |
| JSONL → HTML 报告 | `uv run --no-project python export_report.py --input output\xxx.jsonl --format html` |
| JSONL → Excel | `uv run --with openpyxl python export_report.py --input output\xxx.jsonl --format xlsx` |
| JSONL → CSV | `uv run --no-project python export_report.py --input output\xxx.jsonl --format csv` |

> 导出 HTML 为**自包含单文件**（ECharts 已内联），双击即看，可任意拷贝分享。
> 看板与导出数据均本地生成，不传云端。

---

*本文档随采集能力扩展持续更新。指标口径如有疑问，以采集器源码注释为准。*
