# 历史看板 UI/UX 评估与优化建议（kimi k3，2026-08-27，v47 / HEAD 3dd3155）

> 评估范围：`web/report.html`（历史看板）为主，`web/index.html`（实时看板）作对照，共用 `web/assets/app.js` + `style.css`。
> 只读评估，未改动任何现有文件。工作量：S=半天内 / M=1~2 天 / L=专门排期。

---

## 总体结论（先给）

**UI 完成度评分：7.5 / 10**

功能层已相当扎实：贯穿线、点击锁定+数据浮层、自写时间拖动条、事件标注、设备信息分行、加载完再显示、列表无感刷新、错误区分提示、legend/曲线/tooltip 三色统一——这些"硬骨头"都已啃掉，交互完成度在内部工具里属上乘。扣分集中在三块：**信息层级无重点**（10 个汇总值平铺、首屏看不到"这次测得好不好"）、**报告的出口断裂**（看完不能导出/分享/发链接）、**视觉噪音**（6 个重复拖动条 + 拥挤的长提示行）。

**最值得先做的 3 件事**：
1. **汇总卡 KPI 语义化分级**（核心 3 项大卡片 + 按指标说明.md 阈值着色）——第一眼回答"这次好不好"。S~M，收益最大。
2. **报告导出/分享入口**（列表项直接打开已生成的 HTML 报告 + URL hash 定位报告）——报告的最终目的是分享与存档，目前看完就断了。S~M。
3. **提示行收敛 + 时间条精简**（长提示行改为 ? 图标悬停展开；6 个重复拖动条减为 1 个全局条）——首屏立刻清爽。S~M。

---

## 一、布局与信息层级

### 1.1 汇总卡 10 项平铺、无 KPI 分级 【高 / S~M / 顺手小改偏 M】
- **问题**：`renderSummary`（app.js:942-961）输出 10 个相同样式的格子（平均帧率/最低帧率/P95 帧率/卡顿率/帧时间 P95/平均进程CPU/峰值内存/平均内存/平均温度/采集时长）。测试工程师看报告第一问是"这次过没过"，现在需要在 10 个格子里自己找数、自己记阈值。
- **建议**：
  ① 核心 KPI（平均帧率、卡顿率、帧时间 P95）做大一号卡片或置顶一行；
  ② 按 `指标说明.md` 现有阈值着色：FPS 满帧±5% 绿 / 30~55 黄 / <30 红；Jank <1% 绿 / 1~5% 黄 / >5% 红；帧 P95 ≤2×刷新周期 绿 / 超 红；温度 <40 绿 / 40~45 黄 / >45 红。色块一眼出结论；
  ③ "采集时长/采样点数"降为灰色小字（元信息，不是 KPI）。
- **依据**：指标说明.md §1/§2/§12 已有现成参考值，无需新造标准。

### 1.2 meta 区主谓不分：文件名喧宾夺主 【中 / S / 顺手小改】
- **问题**：meta 首段（report.html:205-212）是 `20260827_090606/perfdog_20260827_090606.jsonl · 817 个采样点 · 被测: com.tencent.mm`——最重要的"被测应用"排在长文件名后面；有备注（remark）时备注反而只在左侧列表显示，报告区看不到。
- **建议**：报告区第一行改为"备注（无则用文件名）+ 被测应用 + 时间"，文件名/点数/核数降为第二行灰字；v47 的设备信息行（dev-info）保持在其后。

### 1.3 单列 6 图长滚动，首屏信息量低 【中 / M / 可排期】
- **问题**：1080p 屏每张卡 ≈260px（图 220 + 头），6 张 ≈1600px 纵向滚动，首屏只能看到 meta+汇总+1~2 张图；宽屏（≥2560px）横向空间浪费。
- **建议**（按成本升序）：① 图表高度从固定 220px 改为 `clamp(180px, 26vh, 300px)` 视口自适应（S）；② 卡片标题点击折叠/展开，折叠状态记忆到 localStorage（M）——看 2 张关心的图时其余收起；③ 宽屏 ≥1800px 双列网格（L，需评估贯穿线/拖动条/拖拽排序在双列下的行为，不建议近期做）。

### 1.4 操作提示行信息过载 【高 / S / 顺手小改】
- **问题**：drag-hint（report.html:35）一行塞了 6 条操作说明（拖卡排序/悬停贯穿/点击锁定/再点双击解锁/时间条三种操作），约 80 字常驻，没人读得完；且缺少两条高频交互说明：**legend 可点击隐藏曲线**、**虚线=日志事件标注**。
- **建议**：默认只留一行短提示（如"悬停看贯穿线 · 点击锁定 · 底部时间条缩放"），其余收进右侧"?"图标 hover/click 展开的操作卡；首次访问展开、之后折叠（localStorage 记忆）。同时补 legend 与事件虚线两条说明。

### 1.5 左侧列表：备注重复显示 【低 / S / 顺手小改】
- **问题**：run-item 渲染（report.html:135-139）中 title 已是 `备注　(文件名)`，下面又单独一行 `📝 备注`——同一备注显示两次，列表项 3 行高、信息密度低。
- **建议**：title 只放备注（或文件名），去掉重复的 run-remark 行；meta 行（时间/点数/大小）保持不变。列表项从 3 行减到 2 行，一屏能多显示 1~2 条记录。

---

## 二、视觉

### 2.1 图表字号/高度不适配大屏与投屏 【中 / M / 可排期】
- **问题**：坐标轴 10px、legend 11px、统计栏 12px——笔记本 1080p 可读，但投屏/会议室场景（团队评审报告的常见场合）基本看不清；2560px 宽屏下 220px 高的曲线被压扁，波动细节丢失。
- **建议**：① 图表高度视口自适应（同 1.3①）；② 加一个"演示模式"开关（页面右上角）：图表高度 ×1.5、字号 +2~3px、隐藏左侧列表与提示行——一键进入投屏状态（M）。

### 2.2 统计栏颜色无语义 【低 / S / 顺手小改】
- **问题**：`.stat-line` 统一黄色 `#ffd54f`，恰好与"进程 CPU"曲线同色——在 CPU 卡上是巧合的呼应，在其余 5 张卡上与任何曲线色都对不上，黄色在深色底上也有"警告"的误暗示。
- **建议**：改为中性浅灰（如 `#b0bec5`）或跟随该卡主曲线色（FPS 卡用 `#4fc3f7`）。

### 2.3 温度卡 Y 轴 min:25 硬编码 【中 / S / 顺手小改】
- **问题**：renderTemp（app.js:847）`min: 25`——冬天/空调房/散热好的机型电池温度可能低于 25°C，曲线直接贴底截断，且该 yAxis 未开 `scale: true`。
- **建议**：改为 `scale: true` 或 `min: function 动态取数据最小值-2`。

### 2.4 微交互反馈 【低 / S / 顺手小改】
- **问题**：run-item hover 只有底色变化无过渡；图表卡无 hover 态；列表 5s 轮询虽有签名检测不闪动，但新记录出现时无任何提示。
- **建议**：run-item 加 `transition: background .15s`；有新记录流入时列表顶部短暂出现"有 N 条新记录"小胶囊（点击滚到顶部）。

---

## 三、交互

### 3.1 时间拖动条 ×6 重复 【高 / M / 建议排个小期】
- **问题**：6 张图各挂一个拖动条（createTimeSliders，app.js:364-383），全部联动同一窗口——功能没错但视觉重复 6 次，且每个都要画一遍迷你波形（大报告时 6 份 canvas 绘制开销）。PerfDog 云端是**一个**全局时间条。
- **建议**：改为单个全局拖动条，位置二选一：① 汇总卡下方 sticky（滚动时跟随，随时可拖）；② 页面底部 sticky。各图下方的波形可保留为纯波形预览（不可拖）或直接移除。
- **注意**：拖动条全联动逻辑（_dz 状态机）可完全复用，只是挂点从 6 减 1，波形绘制从 6 减 1，工作量主要在布局与 sticky 细节。

### 3.2 时间条不支持触屏 【中 / S~M / 顺手小改】
- **问题**：_bindSlider 只绑了 mousedown/mousemove/mouseup（app.js:409-459），`.pd-slider` 虽有 `touch-action:none` 但没有 touch 事件绑定——平板/触屏笔记本/手机浏览器看报告时时间条完全无法操作（贯穿线点击倒是能用）。
- **建议**：把事件抽象成 pointer events（pointerdown/move/up）或补 touchstart/touchmove/touchend 映射到同一套 mode 逻辑。

### 3.3 窄屏选记录后无滚动引导 【中 / S / 顺手小改】
- **问题**：≤900px 断点下列表在上（max-height 200px）、报告在下（style.css:212-215），点记录后报告区在视口外加载完成，用户不知道下面已经有内容了。
- **建议**：该断点下 selectRun 渲染完成后 `report-body.scrollIntoView({behavior:'smooth'})`。

### 3.4 原生 prompt/alert 与深色主题割裂 【中 / M / 可排期】
- **问题**：重命名用 `window.prompt`（report.html:153）、读取失败/重命名失败用 `alert`——原生白底弹窗与深色看板视觉断裂，且 alert 打断操作流（看报告时被打断很突兀）。
- **建议**：做一个极简的深色 toast 组件（右下浮层 2s 自消）替换所有 alert；rename 换内联编辑（点 ✎ 后标题变输入框，回车提交 Esc 取消）。toast 可先只替换"失败类"提示，rename 内联化可后做。

### 3.5 事件标注层缺少图例说明 【低 / S / 顺手小改】
- **问题**：renderEvents 在 6 张图上画虚线（仅 FPS 卡带文字标签），没有任何地方说明"虚线=logcat 事件"——新用户看到一堆蓝色/红色虚线不知所云。
- **建议**：并入 1.4 的操作卡说明；或在 FPS 卡 legend 区加一个小图例"┄ 日志事件"。

### 3.6 锁定后的跨图时刻汇总 【低 / M / 可排期】
- **问题**：点击锁定后 6 张卡各自在标题行显示 📌 数据（v46 已按数值降序），信息分散在 6 处，想抄一个时刻的全指标要扫 6 张卡。
- **建议**：锁定期间在页面顶部（meta 下方）加一条"T=xx.x s 全指标快照条"（FPS/Jank/P95/CPU/内存/温度一行流），解锁即消失。

---

## 四、可用性

### 4.1 报告导出/打开入口断裂 【高 / M / 建议排个小期】
- **问题**：采集结束自动生成的自包含 HTML 在 `output/<时间戳>/` 里，但看板里没有任何入口——用户要分享报告得：记路径 → 开文件管理器 → 找文件夹 → 双击。命令行导出（export_report.py）对测试同学门槛高。
- **建议**：① web.py 加一个只读端点伺服 output 下的 `.html`（复用 `_load_report` 的防穿越校验：`/api/raw?name=xxx/perfdog_xxx.html`）；② 列表项 hover 出"📄 打开报告"小图标，新标签页打开；③ 顺手加"📥 CSV"（调 export_report 同款 flatten，服务端直接生成返回）。
- **价值**：这是"报告"二字的闭环——看板内看完 → 一键拿到可分享的文件。

### 4.2 报告 URL 不可分享 【中 / S / 顺手小改】
- **问题**：selectRun 不更新地址栏，刷新/转发链接后回到未选中状态，团队里"你看这一份"只能靠口述文件名。
- **建议**：selectRun 时 `history.replaceState` 写 `#name=<encodeURIComponent(name)>`，页面加载时读 hash 自动选中。10 行以内的事。

### 4.3 双报告对比缺失 【中 / L / 专门排期】
- **问题**：版本回归的核心场景是"这次 vs 上次"（指标说明.md §7 也建议同场景对比），现在只能开两个标签页人肉比。
- **建议**：分两期：① 轻量版（M）——列表多选 2 条 → 汇总卡并排两列 KPI 数值 + 差值着色（绿=改善/红=退化）；② 完整版（L）——双报告曲线同图叠加（统一 t_ms 轴，主报告实线/对比报告虚线）。轻量版已能覆盖 80% 回归对比需求。

### 4.4 实时看板停止后缺少"看本次报告"直达 【低 / S / 顺手小改】
- **问题**：index.html 点"⏹ 停止采集"后 tb-status 只显示"✔ 已停止"，要去历史页找本次记录（列表最上面那条，但新人不知道）。
- **建议**：停止成功后 tb-status 追加一个"查看本次报告 →"链接（`/report.html#name=<run_dir>/perfdog_<run_id>.jsonl`，配合 4.2 的 hash 定位正好闭环）。

### 4.5 大报告（>3000 点）传输与渲染 【低 / M / 观察项】
- **问题**：/api/report 全量 JSON 返回 + 前端全量 setOption + 6 份波形 canvas。817 点（~800KB）无感；1 小时长测（3600+ 点，~4MB）会有一次性卡顿。
- **建议**：暂不动；真遇到长测场景再做服务端降采样（>2000 点按 lttb/隔点抽稀，拖动条波形用抽稀数据）。

### 4.6 统计数据含静止段噪音 【低 / S / 顺手小改】
- **问题**：汇总卡"最低帧率"与 stat-line 的"最低"把静止画面 FPS=0 算进去（静止是合法状态，README 已说明），最低 0.0 没有信息量反而误导。
- **建议**：统计时过滤 `fps==0 且 stale` 的点（jsonl 有 stale 标记），或显示"最低（除静止）"。

---

## 五、响应式 / 多屏

### 5.1 ≤900px 列表仅 200px 高 【低 / S / 顺手小改】
- **问题**：窄屏下列表被压到 200px（约 3 条记录），筛选长列表很痛苦。
- **建议**：窄屏下列表改为可折叠抽屉（默认收起，点"采集记录 ▾"展开），或 max-height 提到 40vh。

### 5.2 触屏整体可用性 【中 / M / 与 3.2 合并做】
- 拖动条 touch 支持（3.2）之外，点击锁定在小屏上尚可，但 .pin-tip 浮层 max-width 220px 在小屏可能贴边，建议加 `max-width: min(220px, 60vw)`。

### 5.3 投屏/演示场景 【中 / M / 与 2.1 合并做】
- 即 2.1 的"演示模式"：大字号 + 隐藏列表 + 隐藏提示行。团队评审时是真实高频场景，值得做。

---

## 六、汇总清单（按优先级）

| # | 项目 | 优先级 | 工作量 | 类型 |
|---|---|---|---|---|
| 1.1 | 汇总卡 KPI 分级 + 阈值着色 | 高 | S~M | 顺手小改偏 M |
| 1.4 | 提示行收敛为 ? 操作卡（补 legend/虚线说明） | 高 | S | 顺手小改 |
| 3.1 | 6 个时间条 → 1 个全局条 | 高 | M | 排个小期 |
| 4.1 | 报告打开/导出入口（/api/raw + 列表按钮） | 高 | M | 排个小期 |
| 1.2 | meta 区重排：备注/被测应用前置 | 中 | S | 顺手小改 |
| 2.3 | 温度 Y 轴 min:25 硬编码 | 中 | S | 顺手小改 |
| 3.2 | 时间条触屏支持（pointer events） | 中 | S~M | 顺手小改 |
| 3.3 | 窄屏选记录后滚动引导 | 中 | S | 顺手小改 |
| 4.2 | URL hash 定位报告 | 中 | S | 顺手小改 |
| 2.1+5.3 | 图表自适应高度 + 演示模式 | 中 | M | 可排期 |
| 3.4 | toast 替换 alert / rename 内联编辑 | 中 | M | 可排期 |
| 4.3 | 双报告对比（轻量版先行） | 中 | M~L | 专门排期 |
| 1.3 | 卡片折叠 / 双列网格 | 中~低 | M / L | 折叠顺手做，双列缓行 |
| 1.5 | 列表备注去重 | 低 | S | 顺手小改 |
| 2.2 | stat-line 颜色去黄 | 低 | S | 顺手小改 |
| 2.4 | 微交互（过渡/新记录提示） | 低 | S | 顺手小改 |
| 3.5 | 事件虚线图例 | 低 | S | 并入 1.4 |
| 3.6 | 锁定时刻全指标快照条 | 低 | M | 可排期 |
| 4.4 | 停止后"查看本次报告"链接 | 低 | S | 顺手小改（依赖 4.2） |
| 4.5 | 大报告降采样 | 低 | M | 观察项 |
| 4.6 | 统计过滤静止段 | 低 | S | 顺手小改 |
| 5.1/5.2 | 窄屏列表抽屉 / pin-tip 宽度 | 低 | S | 顺手小改 |

**顺手小改包（一次提交可带走）**：1.4、1.2、1.5、2.2、2.3、3.3、4.2、4.4、4.6、5.1——全是 S 级，建议打包成"UI 细节一轮"。
**排期项**：3.1（全局时间条）、4.1（导出闭环）、2.1+5.3（演示模式）、4.3（对比轻量版）。
