# 评估报告：指标算法评审 + 真机报告数据诊断（kimi k3）

> 评估模型：kimi k3（Moonshot）
> 评估日期：2026-08-21
> 数据基础：F1=20260821_131418（807 点 / 807s）、F2=20260821_083608（1281 点 / 1282s），均为荣耀 ADT-AN00 + 微信小游戏，pid 全程不变（1697/5838），无 target 切换。
> Schema：`fps{layer,fps,jank_rate,refresh_hz,frame_p50/p95/max_ms}` / `cpu{pid,cpu_total_pct,cpu_proc_pct}` / `mem{pss_kb,vmrss_kb}` / `net{rx_kbps,tx_kbps}` / `therm{temp_c,voltage_v}`（荣耀机 current/power 全程 null，/sys 无权限已降级，符合预期）。

---

## 第一部分：指标算法评审

### fps.py — 总体合理，4 个注意点
- ✅ span 法对 128 槽滚动缓冲稳定；INT64_MAX 哨兵过滤；以"最新帧时间戳是否推进"判静止（帧时间戳与 uptime 基准不同，设计正确）；`_ever_surfaceview` 防误切 gfx 通道。
- ⚠️ ① **窗口重叠重复计数**：128 槽 @60Hz ≈ 2.13s 数据窗 > 0.5s 采样间隔，相邻采样共享大量相同帧，同一次卡顿被多个点重复计入 jank_rate，聚合均值偏高；② 暂停恢复后 span 跨暂停间隙，恢复初期 FPS 偏低 + 多记一次 Jank（口径特性）；③ 单列格式兜底 refresh=60Hz，高刷设备 Jank 阈值会偏宽；④ sf/gfx 双通道 Jank 口径不一致（gfxinfo 是系统 90 分位阈值），不宜横向比。

### cpu.py — 合理，2 个解读注意
- ✅ 差值法 + CLK_TCK 动态校准 + rfind(")") 防进程名含空格，均正确。
- ⚠️ 进程 CPU 单核折算可 >100%（实测峰值 158.98% = 1.6 核），报告读者易误读；整机% 是全核平均，大小核架构下不能直等同功耗。

### mem.py — 合理
- PSS 主口径与 PerfDog 一致；兜底正则 `TOTAL\s+(\d+)\s+kB` 仅在主正则失败时启用，误匹配风险低。

### network.py — 口径级问题（重点）
- ⚠️ `/proc/<pid>/net/dev` 是 net namespace 视图，Android 应用共享同一 net ns → **读到的是整机流量，不是进程级**。前台小游戏占绝大部分流量时近似可用，但微信主进程心跳、系统后台同步均被计入。Android 10+ 免 root 无严格进程级方案，建议修正文档口径说明、算法保留。

### thermal.py — 合理，数据实证传感器局限
- ✅ 双通道降级 + sys_unavailable 锁定，好设计。
- ⚠️ 实证：dumpsys battery 温度更新慢、1°C 量化（F1 每 ~50 点变一次、F2 每 ~107 点变一次）→ 曲线阶梯状、滞后于真实热事件，且是电池温度≠SoC 温度。建议补充 `/sys/class/thermal/thermal_zone*`。

### main.py 调度 — 1 个观察点
- fps 线程 0.5s 采样但主线程 1s 落盘，一半 fps 样本被丢弃，jsonl/SSE 实为 1s 粒度；若要 0.5s 粒度 Jank 实时性，落盘推流也需提速。

---

## 第二部分：数据诊断

### 问题 1：CPU 后期波动频繁的原因

**结论：主因是游戏侧后期发生了真实重负载场景事件，热降频/调度抖动是放大器，采集侧可排除。**

波动量化：CPU 整机后期均值并未升高（F1 39.14→41.29；F2 34.03→34.29），**是方差变大而非均值漂移**——进程 CPU 标准差 F1 前 13.88→后 18.56、F2 前 11.80→后 16.42。

- **游戏侧 · 置信度 高**：F1 在 **729~737s** 出现典型"场景事件指纹"：进程 CPU 尖峰 158.98%（733s）+ PSS 尖峰 766MB（平时 537MB）+ 网络突发 980KB/s + Jank 0.22~0.51 连续 6 秒，三者同刻冲高 = 场景切换/资源加载/GC 综合特征。F2 在 **1258~1279s** 同样：网络 2442KB/s、帧 Max 777ms、Jank 0.78~0.92、FPS 跌到 0→28.94。对照组：F1 t=80s frame_max 1988ms 时 CPU 仅 7~22%（加载等待型卡顿不耗 CPU），后期尖峰是 CPU 主动冲高型，性质不同。
- **热降频放大 · 置信度 中高**：F2 从 737s 起温度钉死 43.0°C 长达 545 点，**后期全部 Jank>0.2 与 FPS<30 都落在 43°C 段**；F1 的 729~734s 高 Jank 也在 43°C。DVFS 调频+大小核迁移使同样工作量墙钟忽快忽慢 → 1s 差值法方差放大。无频率数据直接证明，故不到"高"。
- **采集侧 · 置信度 低（排除）**：采集负载恒定无后期特异性；pid 全程不变排除进程重启；158.98% 尖峰量级远超 adb/dumpsys 自身开销（个位数%）。

### 问题 2：无内存泄漏，温度为何大幅上升（29/30°C → 43°C）

**先纠正前提：内存泄漏与发热无因果关系。发热 ∝ 功耗，不 ∝ 内存占用。** PSS 平稳（F1 末段 548MB 属场景增长；F2 末段 476MB 甚至低于首段 537MB）只说明内存健康，推不出"不该热"。

**结论：主因是 GPU 持续满帧渲染 + CPU 持续中等负载 + 屏幕常亮，13~21 分钟稳态功耗把电池温度推到热平衡 43°C，与内存无关。**

1. **GPU 持续满帧渲染 · 置信度 高**：FPS 全程 57~59.86 → WebGL 每 16.7ms 提交一帧、GPU 几乎无空闲渲染全程。满帧渲染是手机最大热源之一，且恰好在 CPU 指标里不可见——正解释了"CPU 不算爆、机器却很热"。
2. **CPU 持续中等负载 · 置信度 高**：整机 33.5~42.5% 全程不降（非瞬时），8 核 SoC 跑 10+ 分钟足以升温 13°C。
3. **热平衡平台 · 置信度 高**：F2 在 737s 后温度钉死 43.0°C = 发热=散热稳态（或温控限功率），持续高负载典型曲线；F1 结束才 41°C，13.5 分钟尚未跑满升温过程。
4. **屏幕常亮 60Hz · 置信度 中**：稳定功耗来源，数据无法分离，按典型占比列第 4。
5. **放电内阻热（排除充电热）· 置信度 高**：电压单调下降（F1 4.37→4.30V；F2 4.35→4.27V）= 纯放电，排除"边充边玩"。
6. **采集器开销 · 可忽略**：<1~2% CPU，不构成热源。

次要佐证：43°C 后 F2 出现 Jank 0.92 / 帧 Max 777ms，F1 出现 Jank 0.51——"发热→调度收紧→帧不稳"链条已出现，临近指标说明 §5 的 45°C 降频阈值。测量口径提醒：电池温度滞后且量化，实际 SoC 温度更高。
